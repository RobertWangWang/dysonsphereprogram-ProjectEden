# 物品品质 — 设计稿 V1

> 状态：**设计稿，未实现。** 本文里标「实测」的都是从 `Assembly-CSharp.dll` 里量出来的，
> 标「旋钮」的是平衡取向而不是推导，标「未决」的是还没拍板的。
> 按本仓库的规矩：**本文里的任何一句话都只是主张，直到 IL 被重新读过。**

---

## 一、一句话

每一批货带着一个**可加的品质点数**；合成时点数跟着走，建造时点数**沉淀进建筑**，
让那台建筑的「材料决定的那些上限」提高，顶尖档 **+30%**。

玩家看到的是 **普通 / 优秀 / 顶尖** 三档，但**内部永远是连续分数** —— 三档只是显示分层。

---

## 二、为什么只能是「可加点数」，不能是「三个标签」

这一节是整个设计的地基，它不是偏好，是结构。

**实测**：全游戏装着物品的地方，结构一律是 `(itemId, count, 载荷)`：

```
Cargo                  { stack, inc, item, position, rotation }
StorageComponent.GRID  { itemId, filter, count, stackSize, inc }
StationStore           { itemId, count, inc, localOrder, remoteOrder, max, ... }
```

`AssemblerComponent.served[]/incServed[]`、`LabComponent.matrixServed[]`、
`InserterComponent.itemCount/itemInc`、`PowerGeneratorComponent.fuelCount/fuelInc`、
机甲背包、配送包、运输船、运输机 —— 24 个结构，全是同一个形状。

**一格一个 `itemId`、一个 `count`。** 所以 100 个普通铁块和 50 个顶尖铁块进同一个箱子，
引擎只有两条路：要么合成一格（品质必须能被平均掉），要么占两格（那就必须是两个不同的
`itemId`，也就是 3N 个 proto）。

传送带是唯一例外 —— `Cargo` 一件一个 `item`，三种品质能同时在带上跑。
**但它们一进箱子 / 物流站 / 装配机，就合并成一个 `(itemId, count, 载荷)`，离散品质当场消失。**

`inc` 之所以能在这 24 个地方活下来，正是因为它是**可加量**：合并时相加、
等级 = `载荷 / count`、拆分时按比例分，信息不丢。品质要活下来，必须是同一种数学。

> **推论（重要，第六节会再用一次）：可加平均是个混合过程，混合是熵增的。**
> 只靠加权平均，全图的品质会自己趋同到一个常数。**必须有一道提纯工序，否则这个系统
> 会在几小时内把自己抹平。**

---

## 三、数据层

### 3.1 字段与宽度

新增一个和 `inc` 平行的字段（暂定名 `qua`），**Int32**，加在那 24 个结构的每一个上。

**为什么是 Int32 而不是 Int16**：品质点数 = `件数 × 单件分数`。
物流站单格容量本仓库配到 **10,005,000**，单件分数上限取 100 →
`1.0005e9`，对 Int32 上限 `2.147e9` 留 **2.1 倍余量**。Int16 在这个量级下连一格都装不下。

**「单件分数 0–100」是从上面这条推出来的，不是拍的**：Int32 除以最大单格容量，
再留一倍安全余量，就是 100 这个量级。启动时要按 `stations.json` 里**实际配的**容量
重算一遍上限并打印推导，和 `PilerLevelPatches.AbsoluteMax` 同一个做法 ——
**容量一旦被调到 2100 万件以上这个字段就会溢出，那必须是一条报警而不是静默。**

`Cargo` 单独说：**实测**加宽后的布局是
`stack(Int16)@0, inc(Int16)@2, item(Int16)@4, 空洞@6-7, position@8, rotation@20` = 36 字节。
放一个 Int32 会把结构推到 40 字节。GPU 那边不用管 —— `UploadRepacked` 本来就重打包成
32 字节的渲染结构，而且**偏移是 `Marshal.OffsetOf` 运行时取的，当初就是为这种情况写的**。

### 3.2 语义（三条，全部照抄 `inc`）

| 操作 | 规则 |
|---|---|
| 合并 | `qua₁ + qua₂`，`count₁ + count₂` |
| 拆分 / 取走 n 件 | 按比例 `qua × n / count`，**先算比例再扣数量** |
| 单件分数 | `qua / count` |

第二条的顺序是硬要求：本仓库在 `MegaVirtualLogisticsPatches` 上栽过一次 ——
「扣了数量没扣 `inc`，剩下的货背走了整堆的点数」，等于凭空造点数。
**每一处 `count` 的改动都必须配一处 `qua` 的改动，比例取在扣减之前。**

---

## 四、搬运层：一遍 Cecil 孪生

### 4.1 实测的分类

**这一节的数字是 `QualityFieldAnalyzer` 跑出来的**（`tools\verify_quality.ps1` 驱动，
只读不写、不碰游戏安装）。初稿里那组数（24 字段 / 661 处 / 21 手工 / ~90 参数）
是用一次性 PowerShell 粗探针估的，把 UI 混在搬运和混合两类里、参数判据也过松，
**以下这组才是准的**：

| 类别 | 方法数 | 访问数 | 处理 |
|---|---|---|---|
| **纯搬运** | 190 | **592** | 机械孪生 |
| **手工** | 19 | 91 | 逐个写，见 4.3 |
| **纯效果** | 22 | 0 | **完全不动** |
| **显示层** | 53 | 145 | 不孪生，阶段四再追加 |

- **载荷字段 30 个**（分布在 23 个类型上）。初稿说 24，少了六个 —— 其中
  `IDCNTINC::inc` 和 `IDCNTMAX::inc` 是反查找出来的真漏（「id + 件数 + 点数」的临时元组）。
- **要长孪生参数的方法 90 个、参数位 104 个，调用点 722 个**（另有 6 个方法属于
  通知汇，按设计跳过，见下）。

  > **这个数字自己就订正过一次，而那次是分析器的真 bug。** 第一版报 55，
  > 因为 `IsIntegerPayload` 判 `Int32&` 时读的是 `MetadataType`，而引用类型的
  > `MetadataType` 是 `ByReference` 不是 `Int32` —— 于是**每一个 `out` / `ref` 的
  > inc 参数都被判成「不是载荷」**。而 out 参数恰恰是「取货时把点数带出来」那一侧，
  > 漏掉它等于品质只进不出，且报告上一切正常。修掉之后 55 → 96，摘掉通知汇后是 90。

- **通知汇整族摘掉**（`Player/DItemNotify::Invoke` / `BeginInvoke`、
  `Player::NotifyPackageAddItem` / `NotifyDeliveryPackageAddItem`、
  `GameHistoryData::OnPlayerPackageAddItem`、`Mecha::OnPlayerPackageAddItem`）。
  它是「背包收到东西了」的事件广播，下游是成就、统计和机甲，没有一个需要知道品质——
  和燃料、弹药同属 B 组的终端消耗。

  > 而摘掉它顺带消掉了 1b 里最麻烦的形状：`DItemNotify` 是个**委托类型**，
  > 给它加参数要同时改委托的两个虚方法和两个 `ldftn` 绑上去的处理器，
  > 而委托签名和处理器差一个参数是**运行时**才炸。实测这一族是整个目标集里
  > **唯一**的虚方法与 `ldftn` 来源 —— 摘掉之后剩下的 90 个全是普通实例/静态方法，
  > 加参数变成纯机械操作。
- **要加版本分支的存档流 17 条**，不是 24 条 —— 有些载荷字段的宿主类型不自己序列化。

**显示层单列是重要的**：那 53 个方法（`UIStorageGrid`、`UIMechaWindow`、`UITurretWindow`、
`UIStationStorage` …）只画箭头和数字、不搬运东西。它们既不孪生也不手工，
所以阶段一根本不碰；要不要让玩家看见品质是阶段四的独立决定。

### 4.2 变换本身

和 `CargoIncWidener` 同一套骨架，**先分析后改写**（`Apply` 跑两遍，只读那遍报出任何
不认识的形状就一个字节都不改 —— 半改写的 `Assembly-CSharp` 是不可恢复的）：

1. 24 个结构各加一个 `Int32 qua` 字段（名字按宿主字段派生：`inc`→`qua`，`incServed`→`quaServed`，`fuelInc`→`fuelQua`…）。
2. 那 ~90 个带 `inc` 参数的方法各加一个平行参数；调用点按**后向栈深度模拟**定位实参边界（这条是上一个 preloader 的教训：参数是表达式不是指令，往回扫操作码会在 `StorageComponent.AddCargo` 上断掉）。
3. 661 处纯搬运访问，每一处发射一条平行指令。
4. 24 个 `Export`/`Import` 各加一个版本分支（`CargoContainer` 已经是 2→3，这次到 4）。
5. **断言**：孪生命中数必须正好 **592**，手工点位必须正好 **19**，一个不多一个不少，否则整个不改。
   这两个数由 `tools\verify_quality.ps1` 给出，改写落地后它同时是验收标准。

> **上一次加宽九成的痛苦不会复现。** `conv.u1` 猎杀、`SweepTruncations`、`FixFeederLocals`、
> `ClampByteWrites` 全是因为「把宽值塞回已有的字节槽」；**新加的 Int32 字段没有任何已有的
> 窄 API 要对付**。换来的新代价是签名增长与调用点补参，那是另一类活，但它会在
> Cecil 写文件时就炸，而不是在游戏里静默丢数据。

`verify_preloader.ps1` 要同步长出新断言（**先长检查器，再长变换** —— 这是上次
`stack` 加宽时记下的规矩）：重读写出来的文件，核对 24 个字段的类型、~90 个签名、
每一处分支目标仍然落在指令边界上。

### 4.2b 声明是定义，启发式只是探测器 —— 这条是写分析器时用一条 Blocker 换来的

第一版把两者搞反了：用「方法里是否引用增产剂效果表」当**判据**去分类。
代价立刻显形 —— `StorageComponent::TakeTailItemsWithIncTable` 引用的 `incTable`
是个**参数**而不是字段，启发式认不出来，于是它差点被当成纯搬运自动孪生，
**而它做的事恰恰是按增产等级分桶取货**，语义上是彻头彻尾的混合。

现在的结构是三条方向独立的规则：

1. 在 `DeclaredMixed` 清单里 → 手工，**无论启发式怎么说**。
2. 启发式认定混合（同时碰载荷和效果表）却**不在**清单里 → **Blocker**。
   游戏更新长出来的新形状绝不能被当成纯搬运——那会把品质静默接到增产剂的效果上。
3. 在清单里却一处载荷都不碰 → **Blocker**。我们在对着不存在的形状写代码。

载荷字段用的是同一个形状：显式清单 + 反查「形状像载荷却不在清单里」的字段。
反查这次命中 15 个，其中 2 个是真漏（已补进清单），13 个是确认的非载荷
（喷涂机的配置字段、`PrefabDesc` 常量、`incoming*` 名字误伤），
记进 `NotPayload` 排除表而不是每次都进噪声 —— 这样反查的输出「非空即有新情况」。
排除表自己也被反向核对：写着而程序集里找不到的，会提示那条理由已经过期。

### 4.3 19 个手工点位，逐个说明

**A 组 · 消耗时按比例扣，并算出产物品质**

| 方法 | 要做的事 |
|---|---|
| `AssemblerComponent::InternalUpdate` | 扣 `served[]` 时同比例扣 `quaServed[]`；产物点数 = 投入点数之和（见五节） |
| `LabComponent::InternalUpdateAssemble` | 同上 |
| `FractionatorComponent::InternalUpdate` | **26 处，最大的一块**。分馏是把一股流拆成两股，品质点数要按件数分到两边 |
| `PlanetFactory::EntityFastFillIn` | 手动塞入 |
| `StorageComponent::TakeTailItemsWithIncTable` | 蓝图铺地基那条路 |

**B 组 · 终端消耗，品质在这里被读走或丢弃**

| 方法 | 决定 |
|---|---|
| `PowerGeneratorComponent::GenEnergyByFuel` | **燃料品质丢弃**。发电的品质是建筑属性（六节），不是燃料属性；两边都给会重复计数 |
| `PowerExchangerComponent::CalculateActualEnergyPerTick` | 丢弃 |
| `TurretComponent::LoadAmmo` | 丢弃（炮塔的品质是建筑属性） |
| `EjectorComponent::InternalUpdate` / `SiloComponent::InternalUpdate` | 丢弃 |
| `Mecha::GenerateEnergy` / `LoadAmmo` / `get_reactorPowerGenRatio` / `get_reactorPowerGenEnhanced` / `get_reactorPowerForWeaponEnhanced` | 丢弃 |
| `MechaForge::GameTick` | 手搓：产物品质 = 投入品质（和装配机同一条规则） |

**C 组 · 明确不参与**

| 方法 | 理由 |
|---|---|
| `SpraycoaterComponent::InternalUpdate` | 喷涂机造 `inc`，**绝不能造 `qua`** —— 否则增产剂变成品质印钞机 |
| `CountInc::get_incArrows` | 纯显示 |
| `FractionatorComponent::get_extraIncProduceProb` | 纯效果读取 |

---

## 五、品质怎么流动

### 5.1 合成：加权平均，而且是免费的

产物点数 = 各投入点数之和，产物单件分数 = 总点数 ÷ 产物件数。

**这一步不用写规则** —— 可加量求和再除以产出件数，本身就是加权平均。
一条吃 2 铁 1 铜的配方，铁 80 分、铜 20 分、出 1 件，产物就是 `(2×80 + 1×20) ÷ 1`，
然后钳到上限 100。

**但它只会稀释，永远不会超过最好的那个投入**（这就是二节末尾那条推论）。

### 5.2 源头与提纯 —— **未决，这是本稿唯一没拍板的部分**

没有源头和提纯，全图会在几小时内趋同到一个常数，整个系统等于不存在。建议：

**源头 · 矿脉品位。** 每条矿脉带一个基础分，采出来的矿就是那个分。
这给勘探一个新理由，而且和本仓库已有的 `RareVeinSurvey` / `RareVeinProspector` 是同一族。

**提纯 · 精选工序。** 一台机器吃 N 件、出 M 件（M < N），**总点数守恒**，
于是单件分数涨了 `N/M` 倍。

选它的三条理由：

1. **在可加模型里它是免费的** —— 就是守恒加上件数减少，不需要任何新规则。
2. **它是真实工艺**：选矿 / 富集（beneficiation）干的正是这件事，代价就是尾矿。
3. **它自带成本和自限性**：要顶尖就得把大量普通料扔掉，而单件分数有 100 的上限，
   所以提纯有天花板、不会失控。

**明确不做的：不让「高品质机器产出高品质产物」。** 那是一个正反馈回路，
几小时后全图饱和，而且它会让效果层和流动层耦合成一团。机器的品质只影响
速度 / 功率 / 耗电，**不影响产物品质**，两条线彻底分开。

---

## 六、效果层：品质沉淀进建筑

### 6.1 建造挂点 —— 材料品质是白拿的

**实测**：五把建造工具加升级加建设机组，全部收敛到同样两个调用：

```
BuildTool_Click / Path / Inserter / Addon / BlueprintPaste :: CreatePrebuilds
PlayerAction_Build::DoUpgradeObject
ConstructionModuleComponent::PlaceItems
        -> StorageComponent::TakeTailItems(..., ref int inc)
        -> Player::UseHandItems(..., ref int useInc)
```

这两个方法**本来就把被消耗掉的那批货的增产点数回吐出来**（它们就在那 ~90 个
带 `inc` 参数的方法名单里），所以孪生做完，同一个调用点同时回吐品质点数。
**建造路径零新增管道，只有 7 个挂点。**

**prebuild → 实体的接力用我们自己的表**：扣料时按 `prebuildId` 记，
`AddEntityDataWithComponents(entity, prebuildId)` 那一刻搬到 `entityId`。
存储沿用 `AlloyRatioStore` 那套（按 `(planetId, entityId)`、进 `IModCanSave`、蓝图能带）。

> **`PrebuildData` 确实有个 `Int32 grade` 字段，不要征用它** —— 那是升级层级。
> 同一个字段两种含义正是陷阱二，而且它进存档。

### 6.2 两条轴

**通用轴 · 耗电。** `PowerConsumerComponent.workEnergyPerTick`，顶尖 **−30%**。
几乎每座建筑都有这个组件，所以「任何建筑用好料都有意义」由这一条保证，一处实现覆盖全部。

> **必须一次性写入，不能每 tick 强制** —— 物流站面板的「最大充能功率」滑条写的是
> 同一个字段，每 tick 压它滑条会弹回去。和 `StationCapacityPatches` 的处理方式一致：
> 只改还停在建造时那个值上的实体。

**本职轴 · 每类建筑一条，顶尖 +30%。** 判据是**必须有冶金上的理由** ——
品质是材料的冶金质量，那它只能改「材料决定的那些上限」。

| 建筑类 | 字段 | 冶金理由 | 陷阱 |
|---|---|---|---|
| 制造（装配/熔炉/化工/精炼/对撞机） | `AssemblerComponent.speed` | 许用应力 ↑ → 转速上限 ↑ | ①巨型建筑靠 `speed >= megaSpeedThreshold` 识别，**只能往上乘**，掉下去那台永远不再被接管；②引擎每 tick 只结算一个周期，已经顶到 60/s 的机器加速无效 |
| 研究站（制造模式） | `LabComponent.speed` | 同上 | **科研速度结构上给不了**：研究模式只读全局 `GameHistoryData.techSpeed`。文档里要写死，否则会被当 bug 报 |
| 采矿 / 抽水 | `MinerComponent.speed` | 同上 | 功率是 `speedDamper × speed² / 1e8`，**平方**；必须走 `AdvancedMinerPatches.ApplyFixedPower` 同一条路，否则顶尖采矿机把电网拉爆 |
| 分拣器 | `stt` / `delay` | 往复件速度上限由疲劳寿命定 | `InserterComponent.speed` 是 **STATIC**，碰它改的是全世界。**这条轴含金量最高** —— 5000 层集装下限制吞吐的本来就是摆速 |
| 发电 | `PowerGeneratorComponent.genEnergyPerTick` | 导热 / 耐热 ↑ → 功率密度 ↑ | **不要碰 `useFuelPerTick`**：ABN_PowerGenerator 判异常的条件正是运行时低于 prefab 的 0.7 倍，而且 `CombustiblePowerPatches.Align` 每 tick 都在重写它 |
| 蓄电器 | `maxEnergy` + `input/outputEnergyPerTick` | 介质纯度 → 能量密度；内阻 ↓ → 充放电率 ↑ | 干净 |
| 电线杆 / 配电 | `PowerNodeComponent.coverRadius` | 导电率 ↑ → 压降 ↓ → 半径 ↑ | 写完必须跑 `OnNodeRemoving → set → OnNodeAdded`，否则平方缓存 `coverRadius2` 和已连消费者全是旧的（`PowerCoveragePatches` 已有这条路）。**`connectDistance` 别动**，连接计算平方增长 |
| 物流站 | `energyPerTick` / `energyMax` / `tripRangeDrones` / `tripRangeShips` / `warperMaxCount` | 充电内阻；结构强度 → 航程 | 干净 |
| 弹射器 / 发射井 | `chargeSpend` / `coldSpend` | 储能与往复件疲劳 → 装填更快 | 干净 |
| 炮塔 | `bulletDamage` | 身管材料 → 许用膛压 | 由 `SetNewItem` 从 `ItemProto.Ability` **每次换弹重写**，挂点必须在那里，只在建造时写一次会被冲掉 |

### 6.3 明确排除的，和理由

**传送带速度 —— 不给。** `BeltComponent.speed` 是逐实体的，`BeltSpeedPatches.Retune`
也已经会连 `CargoPath.chunks` 一起改。但 `CargoPath.Update` 往回扫空位时
**对缓冲区下标没有下界检查**，速度大了在短带子上直接 `IndexOutOfRangeException`，
而看起来像上限的 `kMaxCargoFlowSpeedPerSecond = 120` 是个**零引用的常量**。
把一个崩溃藏在玩家看不见的品质变量后面，比不给这条轴糟得多。

**物流站容量 / 槽位数 —— 不给。** 容量是体积不是材料性质，给了只是数值 buff，没有故事。

**科研速度 —— 给不了。** 见上表。

### 6.4 幅度

顶尖 **+30%**（耗电是 −30%），**线性插值到品质分数**，不是三段跳 ——
内部本来就是连续点数，插值更省事也更平滑。三档只是显示分层：

| 显示 | 单件分数 |
|---|---|
| 普通 | 0 – 33 |
| 优秀 | 34 – 66 |
| 顶尖 | 67 – 100 |

**「+30%」是旋钮不是推导。** 理由只有一条：这些加成会**铺满全图**，
而且第六节里十类建筑各给一条，叠在同一条产线上时是取瓶颈而不是连乘，
但耗电那条是全局连加的 —— 所以幅度必须小。

---

## 七、存档

24 个结构各加一个版本分支。`CargoContainer` 已经因为 `inc` 加宽从 2 到 3，这次到 4；
其余按各自现有版本号 +1。

**存档策略沿用已生效的所有者决定**：存档跟着这个 mod 走 ——
带品质的存档打不开就是打不开，这一条早就认了，`README.md` 里也已经写明。

`PrebuildData` 不动（六节）。逐实体的建筑品质走 `IModCanSave`，
和 `AlloyRatioStore` / `SlotDataStore` / `CatalystBedStore` 同一条路，**SaveVersion 再加一档**
（那条流是位置相关的，`Export`/`Import`/`IntoOtherSave` 必须同步改）。

---

## 八、显示

按优先级排，**每一条都是可选且独立的**：

1. **物品提示栏一行**：`品质 顶尖（82）`。走 `ItemProto.GetPropName/GetPropValue` 那套，
   和 `CombustiblePowerPatches` 的「工作温度」行同一个做法，字段号要接在 78 之后
   并且开机核对区间不重叠（`CheckFieldRange` 已经是这个形状）。
2. **建筑窗口一行**：那台建筑的品质分与它换到的加成。
3. **传送带 / 储物格上的标记**：那 17 个 `UI*` 方法就是干这个的，追加而不是孪生。

---

## 九、分阶段

**阶段一 · 只做数据层和搬运层，效果层留空。**

> **阶段一按风险又切成三刀**，先做最危险的那个未知数：
>
> | | 内容 | 风险 | 状态 |
> |---|---|---|---|
> | **1a** | **27** 个孪生字段 | `Cargo` 36 → 40 字节 | **已完成，已进游戏验证** |
> | **1b** | 合成线程静态寄存器组（4 个 `[ThreadStatic] int`） | 嵌套覆盖 | **已完成并离线验证** |
> | 1c | **主干道** 188 处访问孪生 + 边界读写 + 存档分支 | 48 种形状 | **框架已就位，形状表 57%** |
>
> ### 1c 可以增量落地，而这不是妥协
>
> 变换是两遍式的：分析遍只要遇到形状表里没有的东西，就记下来、**一个字节都不改**。
> 所以「形状表只补了一半」的结果是**变换整个不生效**，游戏与不装 preloader 时一致 ——
> **不存在半吊子状态**。补一条形状，覆盖率涨一点；全齐了，它才会第一次真的改字节。
>
> `tools/quality_1c.ps1` 每次把当前进度和**还缺哪些形状**打出来。
>
> ### 「认得」不等于「做得到」，而这个区别我一开始没做出来
>
> 形状表一度报到 72.6%，但那是**识别率**：`TwinShapes` 里有 16 条，
> 而发射代码一行都没写。两遍式只保证「形状表没齐就不改字节」——
> 它保证不了「表齐了但发射器是空的」。那种情况下变换会**报成功、实际什么都不做**：
> 品质恒为 0，而日志说一切正常。**这正是本仓库最怕的失败形态，而且是我亲手造的。**
>
> 现在拆成两张表：`TwinShapes`（打算孪生）和 `Emitted`（**发射代码已写**），
> 只有同时在两张表里才算真的认得；`Emitted` 里出现 `TwinShapes` 没有的形状也是
> Blocker（那是个永远不会被调用的发射器，看起来像做完了）。
>
> 于是当前的**诚实进度**是：
>
> ```
> really done            42 / 186 = 22.6%
>   已发射                 27   <- ldc stfld:PAY，第一个落地的发射器
>   确认不需要孪生          7
>   明确丢弃                8
>   已识别但没发射器       93
>   形状表还没有的         51
> ```
>
> ### 每写一个发射器就立刻让它真的跑一遍
>
> `Apply` 在形状表补齐之前拒绝改写 —— 这是对的，但副作用是**发射代码一次都不会被执行**。
> 写完四十几个发射器再一起发现全都产出非法 IL，代价太大。
>
> 所以加了 `ApplyPartial`（**只给离线校验用，Patcher 永远不调**）：把已经写好发射器的形状
> 真的发射出去、写盘、重读、断言没有坏掉的分支目标和空操作数。
> `tools/quality_1c.ps1` 每次都跑它。
>
> **而「能写能读」只证明 IL 合法，不证明它对**，所以还要人眼看一次实际发射的结果：
>
> ```
> 0007: ldarg.0
> 0008: ldc.i4.0
> 0009: stfld Cargo::inc        ← 原语句
> 000E: ldarg.0                 ← 前缀原样重放
> 000F: ldc.i4.0
> 0010: stfld Cargo::qua        ← 孪生，紧跟其后
> ```
>
> 三个设计点在这里同时成立：**前缀可证明是纯的**（核心形状里没有 `call:` 就意味着
> 前缀只有载入指令）、**插在原语句之后**所以标签和异常块边界一个都不用搬、
> **倒着插**所以还没处理的 Job 下标不受影响。
>
> **每一种形状必须落进三档之一**，三档之外一律报成未处理：
>
> | 档 | 含义 |
> |---|---|
> | **要孪生** | 发射一条平行语句，`inc` 换成 `qua` |
> | **不需要孪生** | 碰的是载荷<b>数组对象本身</b>（取长度、判空、循环边界），不是里面的点数 |
> | **明确丢弃** | 值送进了主干道之外，没有孪生槽位可去 —— <b>但必须说出来</b> |
>
> 第三档是后加的，而它一加上就逮到两条必须处理的事。变换现在每次都把丢弃点名报出来：
>
> ```
> PilerComponent::InternalUpdate ×4      自动集装机 —— 叠过的货会掉品质
> StorageComponent::Sort ×1              整理箱子会掉品质
> CargoContainer::RemoveCargo ×1         拆传送带
> FactoryStorage::ThrowItems_Storage ×1  拆建筑扔地上
> PlanetTransport::ThrowItems_Station ×1
> ```
>
> **前两条不能留成「已知代价」**：整理箱子是玩家随手就做的操作，集装则是这个 mod
> 传送带的核心（5000 层）—— 在这两处掉品质都会被当成 bug 报。
> 解法是把 `PilerComponent.cacheCargoInc1/2` 加进主干道，以及查清 `Sort` 那一处到底写去了哪。
> 后三条（扔地上、拆传送带）落在 `TrashObject` / `ItemPackage` 上，属于可接受的丢弃。
>
> > **这一档自己也差点变成静默的。** 第一版把丢弃日志写在「形状表没补齐就提前返回」
> > 的后面，于是它在补齐之前从来不打印 —— 正好违反了它自己要执行的那条规矩。
>
> 待补的那 40 种是有归类的，不是散沙：
>
> | 形状 | 处数 | 性质 |
> |---|---|---|
> | `call:split_inc` 三种 | 19 | 值流进函数再流出，品质要自己那份按比例分摊 |
> | `stind.i1` / `stind.i4` | 7 | 通过 out 参数写出去 → **走侧信道**，三条纪律在这里生效 |
> | `call:Write` / `ReadInt32` | 6 | 存档 → 版本分支 |
> | `newarr` / `call:Resize` | 5 | `incServed` 数组分配 → 孪生数组要一起分配 |
> | `ldlen blt/beq` | 4 | **只是取数组长度做循环边界，根本不用孪生** |
> | `div stloc` | 3 | `inc / count` 求等级 → 孪生是 `qua / count` |
>
> `ChannelUses` 这个计数器现在恒为 0，是故意留着的：形状表补齐之后如果它<b>仍然</b>是 0，
> 就说明跨边界那几种形状被识别错了 —— 而那是静默的。
>
> ### 1c 的范围是量出来的，不是估的
>
> 「孪生全部访问」一度看起来是唯一选项，直到做了一次**形状普查**
> （`tools/quality_shapes.ps1`，只读）。把方法体按栈深切成语句、
> 归一化成签名再做直方图，结果是：
>
> | 范围 | 语句数 | 完整形状 | **核心形状**（剥掉寻址前缀） | top 20 覆盖 |
> |---|---|---|---|---|
> | 全部 27 个载荷 | 488 | 139 | **74** | 78.5% |
> | **主干道 4 个载荷** | 186 | 76 | **48** | 81.2% |
>
> **剥掉寻址前缀是关键的一步**：完整形状之所以发散，是因为「怎么走到那个对象」
> （`ldarg` / `ldarg ldfld ldloc ldelem` / `ldloc ldloc ldelem`…）千变万化，
> 而寻址指令是**纯的、可重复执行的** —— 孪生时原样再走一遍就行，
> 完全不用理解它走的是哪条路。真正要镜像的只有「对载荷做了什么运算」。
>
> 全局 74 种、要四十来个模式才到 95%，后面还拖着一次性形状的长尾 ——
> 那是一趟真正的编译器 pass（建值图、复制子图、处理分支汇合），而且是一次性
> 大爆炸式落地，中间没有可验证的中途状态。
>
> **主干道 48 种就是 100%**，是一份有限、可枚举的清单，配上「遇到表外形状整个不改」
> 就是这个仓库做惯的形状。所有者据此把 1c 收到主干道：
>
> ```
> Cargo::inc                      传送带
> StorageComponent/GRID::inc      箱子 / 背包
> StationStore::inc               物流站槽
> AssemblerComponent::incServed   生产投入
> ```
>
> 也就是品质从矿走到建筑的整条路。其余载荷（炮塔、弹射器、发射井、机甲、
> 分馏塔、能量交换器……）明确列为**「品质在此丢弃」**，和燃料、弹药同属 B 组终端消耗。
> **这是范围收窄，不是偷工减料** —— 「品质只在主干道上流动」是一条能对玩家讲清楚的规则。
>
> 那 48 种形状是一套很小的词汇表，值得抄在这里当开工清单：
>
> ```
> ldc stfld:PAY                                        inc = 常数
> ldfld:PAY stloc                                      局部 = inc
> stfld:PAY                                            inc = <栈上的值>
> ldflda:PAY dup ldind.i4 add stind.i4                 inc += x
> ldflda:PAY dup ldind.i4 sub stind.i4                 inc -= x
> ldfld:PAY ldc dup ldind.i4 add stind.i4              数组元素的 inc += x
> ldflda:PAY dup ldind.i4 call:split_inc add stind.i4  inc += split_inc(…)
> ldind.i4 ldfld:PAY add stind.i4                      *out += inc
> ldfld:PAY stind.i1 / call:Write                      写出去（存档 / out 参数）
> ```
>
> 带 `call:` 的那几种正是**跨方法边界**的地方，也就是 1b 那个侧信道要接的点，
> 以及三条纪律要管住的地方。
>
> ### `split_inc` 读透了，孪生是推出来的
>
> `StorageComponent.split_inc(ref int n, ref int m, int p)`（IL 000F–003E）：
>
> ```
> level = m / n ;  rem = m - level * n
> n -= p
> rem -= n
> ret = rem > 0 ? level * p + rem : level * p
> m -= ret ;  return ret
> ```
>
> `n` 件数、`m` 总点数、`p` 取走几件，返回跟着那 `p` 件走的点数。
> **这正是可加量的标准按比例拆分** —— 也就是本设计第二节推出来的那条规则，
> 原版自己就实现了一份，而且连余数分给谁都处理了。所以品质的孪生不用发明：
> 同一套数学作用在 `(件数, 品质点数)` 上。
>
> 已合成 `ProjectEdenQualityChannel.Split(int countAfter, ref int qua, int p)`，
> 逐条对应原版。**不能直接再调一次原版的 `split_inc`：它会顺手把件数减掉**，
> 同一个调用点调两次就会凭空销毁物品。所以合成版收的是「已经减过的」件数，
> 内部加回去还原 `n` —— 孪生语句发射在原调用之后，件数那时正好是减过的。
>
> **调用点的真实 IL 已经读出来了**（`StorageComponent::AddItem @IL_01CB`）：
>
> ```
> ldarg.0; ldfld grids; ldloc V_4; ldelema GRID; ldflda GRID::inc   ← 寻址前缀
> dup; ldind.i4
> ldarg.0; ldarga.s count; ldarga.s inc; ldloc.s V_5                ← split_inc 实参
> call split_inc(int&, int&, int)
> add; stind.i4
> ```
>
> 孪生要发射的是：寻址前缀**原样重放**、`inc` 换 `qua`、`split_inc` 换 `Split`，
> 而 `inc` 那个参数的品质在**侧信道寄存器**里：
>
> ```
> <寻址前缀，把 GRID::inc 换成 GRID::qua>
> dup; ldind.i4
> ldarg.s count                      ← countAfter（值，不是 ref）
> ldsflda ProjectEdenQualityChannel::Q<槽位>   ← ref qua
> ldloc.s V_5                        ← p
> call ProjectEdenQualityChannel::Split
> add; stind.i4
> ```
>
> ### 侧信道把 1b 几乎消解掉了，而那是设计变更的诚实后果
>
> 原来的 1b 之所以是独立的一大刀，是因为「改 90 个签名 + 补 678 个调用点」本身就是工程。
> **侧信道没有签名要改、没有调用点要补** —— 寄存器就是个静态字段，
> 读写它的那些指令<b>本来就是 1c 访问孪生的一部分</b>（`inc` 跨方法边界的地方，
> 品质就写/读一次寄存器）。所以 1b 只剩「把寄存器造出来」。
>
> **四个 `[ThreadStatic] int`，不是数组。** 数组要判空要分配，而 `[ThreadStatic]` 的
> 初始化**只在第一个线程上跑**（C# 的经典陷阱），其余线程拿到 null。四个 int 默认就是 0：
> 没有初始化、没有分配、没有边界检查 —— 这是 tick 路径，仓库的规矩是绝不在那里分配。
> 四个是实测的余量：**单个方法最多 2 个载荷参数位**（`DeliveryPackage.AddItem`），
> 分析器会报这个数，超过 4 就是 Blocker。
>
> **线程静态而不是普通静态**：DSP 的工厂 tick 跑在约 31 个工作线程上，普通静态会被撕碎；
> 一条调用链永远在同一个线程内，所以线程静态既够用又无锁。校验会断言那个特性确实在——
> 漏掉它的表现是品质在高负载下**偶尔**串味，不崩不报错，是这套设计里最难查的失败形态。
>
> ### 1c 必须遵守的三条纪律（侧信道的正确性全靠它们）
>
> 1. **入参方向**：调用方在 `call` 指令**正前方**写寄存器。实参那时已经在栈上，
>    所以求值过程不可能再覆盖它。
> 2. **出参方向**：被调方在**每一条 `ret` 之前**写寄存器，调用方在 `call` **正后方**读。
>    两者之间只隔一次返回。
> 3. **被调方一进来就把入参寄存器读进局部变量，一出去才写出参寄存器** ——
>    中间它自己调别人会覆盖寄存器。**这是唯一真实的窟窿**，靠这条纪律封住。
>
> ### 1a 进游戏时炸了两次，两次都是同一个病：枚举了 A 族，没枚举 B 族
>
> **第一次：结构体被原样喂给 GPU。** 给 `TrashObject` 加一个 Int32，它从 44 涨到 48，
> 而 `TrashContainer.Draw` 的 `ComputeBuffer` stride 写死 44，启动即
> `ArgumentException`，栈里只有 Unity 和渲染方法。
>
> 全模块扫 `ComputeBuffer.SetData` 的实参类型，答案是**四个**载荷结构体：
> `Cargo`（早有 `UploadRepacked`，所以传送带没事）、`TrashObject`、
> `DroneData`、`CourierData`。后三个暂时从清单里摘掉，30 → 27。
>
> **修复不是摘掉，是 `CheckGpuUploads`**：分析器现在自己去找「有没有载荷结构体会被喂给
> GPU」，命中即 Blocker。并且**验证过它真的会响**（临时把 `Cargo` 从已处理名单里拿掉，
> 它立刻拒绝改写）——一个从没响过的检查器只是个主张。
>
> **留下的缺口要说明白**：`TrashObject` 丢品质无所谓；`DroneData` / `CourierData`
> 是运输机在飞的那趟货，是站与站之间的主干道，**1c 必须解决**。
> 可选路线：给这三个也做重打包（新字段追加在末尾，所以**原布局就是新结构体的前缀**，
> 按原 stride 盲拷即可，不用像 `Cargo` 那样逐字段写渲染副本），
> 或给 `StationComponent` 加一条和 `workDroneDatas` 平行的品质数组。
> 分析器每次启动都会把这条缺口打出来，不让它沉默。
>
> ### 第二次：改签名会打死别人的 mod —— 这条推翻了整个 1b
>
> `MegaAssemblerPatches` 调 `PowerGeneratorComponent.SetNewFuel`，而 1b 给它加了尾参，
> 插件按旧签名编译，在并行 tick 线程上抛 `MissingMethodException`。
>
> 修自己很容易（8 处）。但**把 profile 里 9 个插件全扫了一遍**之后，结论变了：
>
> | 插件 | 直接调用被 1b 改签名的方法 |
> |---|---|
> | ProjectEden | 8 |
> | **UXAssist** | **12** |
> | **InstantDelivery** | **2** |
>
> **我改不了别人的 mod，而 InstantDelivery 还是本 mod manifest 里声明的依赖**，
> 被打掉的正是它的全部功能 `TeleportShip`。
>
> 顺带查出一件已经存在的事：UXAssist 那 12 处里有 2 处调的签名
> （`PlanetFactory.InsertInto(…Byte,Byte,Byte&)`、`CargoPath.TryInsertItem(…Byte,Byte)`）
> **早就被 `CargoIncWidener` 改成 Int16 了** —— 也就是说 UXAssist 的「矿脉保护」和
> 「传送带信号购买」在<b>现在这个已发布版本里就已经是坏的</b>，只是没人报过。
> 这不是品质带来的，但它说明 preloader 改签名的兼容代价一直被低估。
>
> **所有者决定：走侧信道，签名一个都不动。** 另外两条路都被否掉：
> 认下代价（InstantDelivery 整个坏掉）、合成旧签名转发方法
> （多一个重载会让任何按名字打补丁的 Harmony patch 抛 `AmbiguousMatchException`，
> 那会当场带走整个 mod，而别的 mod 怎么打补丁我们控制不了）。
>
> **这条决定现在有护栏**：`verify_quality.ps1` 每次都断言
> 「改写后没有任何方法的签名和原版不同」。它一旦失败，就说明这个决定被悄悄推翻了。
>
> **1b 的两处已知代价，先记下来免得开工才发现：**
>
> 1. **本 mod 自己绑着其中三个方法。** `CargoWidening` 为兼容加宽前后两种签名，
>    把 `CargoPath.TryInsertItemAtHeadAndFillBlank` / `CargoPath.TryPickItemAtRear` /
>    `CargoTraffic.TryPickItemAtRear` 绑成了显式委托类型。加参数后那些绑定会失配，
>    巨型建筑的传送带收发会停摆（会走 `catch` 打一条 ERROR，不是静默，但功能是没的）。
>    所以 1b 必须连插件侧的委托一起改，加第三套带品质参数的变体。
> 2. **调用点插参数必须就地改写 `call` 指令本身**，不能在它前面 `Insert`：
>    分支目标是对象引用，指向 `call` 的分支会**跳过**新插的压栈指令。
>    做法是把原 `call` 对象改成第一条压栈指令、再在后面补一条新的 `call`，
>    这样指向它的分支落在压栈上再自然落到 call。配合 `SimplifyMacros` /
>    `OptimizeMacros`，否则方法体变长会把短分支位移截断——那是已经炸过一次的形状。
>
> **1a 刻意一条 IL 都不改。** 参数和访问的改写会动方法体，而动方法体在这个 preloader 上
> 已经炸过两次、两次都只有进游戏才看得见（短分支被撑长后位移截断、零件数上传仍校验 stride）。
> 只加字段是纯元数据改动：**没有分支截断的可能，没有栈形状的可能**，
> 剩下唯一的未知数就是结构体布局，也就是要单独退掉的那个。
>
> 存档读写也不在 1a 改 —— 品质此刻恒为 0 / null，没有东西可存。等 1c 真有代码写这些字段时，
> 再连同数组分配和版本分支一起做。
>
> 落地：`QualityFieldAnalyzer.cs`（只读的闸）、`QualityFieldAdder.cs`（1a 加字段）、
> `QualityParamAdder.cs`（1b 加参数 + 补调用点）、
> `tools/verify_quality.ps1`（离线驱动：分析 → 对副本改写 → 写盘 → **重新读回来**再断言）、
> 运行时 `QualityWidening.Report()`（各种状态都打印，「字段在但管子没接」必须和
> 「preloader 没装」分得开）。
>
> **1b 的三个实现要点，每一条都是已经量过或已经炸过的：**
>
> - **新参数一律追加在末尾**，于是压栈点就是 `call` 指令正前方，
>   完全不需要 `CargoIncWidener` 当初那套后向栈深度模拟（那是因为它要**改**已有参数）。
> - **不能用 `Insert` 实现「正前方」。** 分支目标和异常处理块边界都是对象引用，
>   指向 `call` 的分支在插入后仍指着 `call`，于是**跳过**新压的实参。
>   做法是把原 `call` 对象就地改写成第一条压栈指令，再在后面补其余压栈和一条新 `call`。
> - **只对真正有调用点的 229 个方法体做 `SimplifyMacros`/`OptimizeMacros`**，
>   不对两万多个方法无差别做——既慢又平白扩大出错面。
>
> **验收不是通用栈深度模拟，而是精确形状核对**：我们插入的形状是固定的
> （值型一条 `ldc.i4.0`，引用型三条 `ldc.i4.0 / stloc / ldloca`），
> 所以写盘重读之后，每个目标调用点的正前方必须恰好是这些指令。漏补一个就会被抓住。
>
> **插件侧同时改了**：`CargoWidening` 有三个方法（`TryInsertItemAtHeadAndFillBlank`、
> 两个 `TryPickItemAtRear`）同时在「被加宽」和「加了品质尾参」两份名单上，
> 所以委托绑定从二选一变成**按实际签名三选一**。绑错是运行时 `MissingMethodException`
> 加一条 ERROR，然后巨型建筑的传送带收发停摆 —— 不是静默，但功能是没的。

做完之后**游戏行为零变化**，验收标准是纯守恒：品质点数能从矿一路走到箱子、
走过物流站、存档、读档、拆传送带，总量不增不减。
验收工具照抄 `CargoLedgerProbe` —— 那个探针当初就是为这件事写的，
实测跑过带拆除、id 回收和多次存读，`对不上 0／残留 0／账本过短 0`。
**这一阶段能独立验证，是整个功能唯一一次能把风险关在门外的机会。**

**阶段二 · 源头与提纯**（五节的未决项拍板之后）。

**阶段三 · 效果层**：7 个建造挂点 + 两条轴。

**阶段四 · 显示。**

---

## 十、已知风险

1. **两次栽在同一处的前科**：上一个 preloader 有两次崩溃都是**离线工具看不见**的
   （短跳转被撑长截断、零件数上传仍校验 stride），只有进游戏才炸。
   `verify_preloader.ps1` 能证明重写结构合法，**证明不了运行时集成**。这条不会变。
2. **品质建成即永久**，没有衰减。玩家最终会把全图重建成顶尖 ——
   这没问题（是个进度），但意味着提纯的成本必须够高，否则它只是个一次性流程。
3. **蓝图粘贴不带材料**，粘出来的品质取决于玩家当时背包里有什么。
   自然结果，但要写进 `mod特性.md` / `mod_feature.md`，否则会被当 bug 报。
4. **拆除退料还不还品质** —— 未决。退（可反悔）还是不退（沉没成本），是平衡取向。
5. **`qua` 的上限和物流站容量共用一笔预算**（三节）。容量配到 2100 万件以上就溢出，
   必须是开机报警。

---

## 十一、这份稿子里还没定的

| # | 未决 | 在哪节 |
|---|---|---|
| 1 | 源头（矿脉品位）与提纯工序的具体形态 | 五节 |
| 2 | 拆除是否退还品质 | 十节 |
| 3 | 提纯工序放在新建筑还是已有建筑上 | 五节 |
