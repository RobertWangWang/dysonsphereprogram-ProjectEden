# 生物矩阵设计稿 V1

> **第七种科研矩阵。** 由菌丝基体与菌落培养而成，
> 凡是需要宇宙矩阵的科技，一并需要它。

物品与配方已实现（`ores.json`，物品 **6007**），
本稿记的是**把它接进科研体系**要动哪些地方。

> **物品号必须是 6007，不能是别的。** 槽位 = `itemId − 6001`
> （`PlanetFactory.InsertInto` IL 03F6），所以矩阵的物品号必须从 6001 起连续。
> 初版给的 6644 落在门外：`6644 − 6001 = 643`，被当场丢弃且一声不吭。

---

## 一、引擎侧勘察结果

全部由 IL 读出，下面的分层就是实现的顺序。

### 1.1 数据层——已经是数据驱动的，改表即可

| 位置 | 现状 |
|---|---|
| `LabComponent.matrixIds` | `static int[6] = {6001…6006}`，**权威表** |
| `TechProto.matrixIds` | 与上表**共用同一个静态数据块**（哈希 `E4C7F588…`） |
| `LabComponent.matrixPoints` | `static int[6]`，每种矩阵的点数 |
| `LabComponent.SetFunction` | `new int[matrixIds.Length]` 开 `matrixServed` / `matrixIncServed` ✓ |
| `UILabWindow._OnInit` | 同样按 `matrixIds.Length` 开 `matrixProtos` / `matrixRecipes` ✓ |
| `Export` / `Import` / `TakeBackItems_Lab` / `ThrowItems_Lab` / `PlanetFactory.InsertInto` | 全部走 `ldlen` ✓ |

**所以不需要 preloader。** 按 CLAUDE.md 那条判据的第二问「数组会不会自己定容」——会。
要做的只是把两张静态表从 6 换成 7。

### 1.2 展开层——三个方法把六个槽写死了

把**全程序**碰这四个数组的方法列了一遍（28 个），用字面下标 0–5 的只有三个：

| 方法 | 触碰次数 | 作用 |
|---|---|---|
| `LabComponent.InternalUpdateResearch` | 45 | 研究结算 |
| `LabComponent.UpdateOutputToNext` | 42 | 叠放实验室之间传矩阵 |
| `LabComponent.UpdateNeedsResearch` | 6 | 告诉分拣器还要不要货 |

**和物流站那次是同一个形状**——CLAUDE.md 记着「原版把 `AddItem` 与四个供需查询在
`storage[0..5]` 上展开，超过 6 格货物就凭空消失」，解法也一样：转译成完整扫描。

**名单是封闭的**：除这三个之外没有第四处用字面下标，所以不会再冒出一个。

### 1.3 界面层——不会崩，但可能看不见

`_OnUpdate` 里三个物品循环**全部以 `itemButtons.Length` 为界**，
而 `matrixProtos` / `matrixRecipes` 是在循环体内被索引的。所以：

> 控件数少于矩阵种类时，只会**少画一格**，不会越界。

**这一条是没走「先加了看崩不崩」那条路的原因。**

`itemButtons` 等六个控件数组**在代码里从不赋值**，全来自 prefab，
而 prefab 在 `resources.assets` 里——**离线读不到长度**。
这正是 `UIEntityBriefInfo.icons` 把物流站钉在 5 格的同一个形状：
**格子数不是逻辑定的，是美术资源定的。**

`LabWindowSurvey` 就是为这一个未知数放的，它同时量了相邻两格的间距——
要克隆第七套控件时照它摆（`MultiProductUIPatches` 那次的教训：间距必须量，不能按表推）。

### 1.4 存档层

`LabComponent.Import` 按**存档里存的长度**开数组，所以老档里的实验室是 6 长。
和 `StationExpandPatches` 给存量物流站扩数组一样，读档后要补齐（陷阱一）。

### 1.5 其它认 6 的地方

- `LabComponent.matrixShaderStates`（`float[10]`）、`techShaderStates`（`int[51]`）——
  实验室里矩阵的 3D 动画，第七种没有对应项。
- `PropertySystem` 自己的 `int[6]` 与 `Realize6006AsVariousMatrixToInventory`
  （宇宙矩阵换各种矩阵）。

---

## 二、接进科技树的方案

> **后续变更（见 §六）**：这一节描述的「改原版科技」最终被 owner 砍掉，
> 现在是 `lab.json` 的 `bioMatrixInTechs`，**默认 false**。
> 生物矩阵改为只经由宇宙矩阵的配方成为终局需求，只算一遍。
> 本节保留，因为它记的是当初为什么选这条路，而那套推理在开关翻回 true 时仍然成立。


**改原版科技：凡是要宇宙矩阵的科技，追加生物矩阵。**

`TechProto.Items` / `ItemPoints` 是每个科技各自的数据，在 `resources.assets` 里，
所以做法是在 `PostAddDataAction` 里遍历 `LDB.techs`，
对 `Items` 含 6006 的那些追加一项。

选这条而不是「新增自己的科技节点」，三个理由：

- **它自动落在终局。** 用宇宙矩阵的科技本来就是最后那一批，
  而莫桑石是「跑遍外星系才找得到」的矿——两者的位置天然对得上，不用另外设计门槛。
- **不用碰科技树布局。** 新增节点要给 `Position`、前置、图标，还要和原版的树不打架。
- **规则可以一句话说清**，玩家不用查表：*白色矩阵要用的地方，现在多要一样活的。*

### 2.1 追加的点数怎么定

`ItemPoints` 是每种矩阵在该科技里的**份数**。原版终局科技对六种矩阵通常给同一个数。
**第一版取「和宇宙矩阵同点数」**——即需要多少宇宙矩阵就需要多少生物矩阵。
它是否过重要看实际产线，而那取决于莫桑石的产量，得在游戏里量。

### 2.2 一条必须守住的

追加必须是**幂等**的：`PostAddDataAction` 在热重载时会重跑，
同一个科技被追加两次就会要双份。按 `Items` 里已含该 ID 就跳过来挡。

---

## 三、实现顺序

1. ~~放探针~~ —— 已做（`LabWindowSurvey`）。
2. ~~两张静态表 6 → 7，`matrixPoints` 同步扩~~ —— 已做（`BioMatrixPatches`）。
3. ~~三个展开方法转译成完整扫描~~ —— 已做（`BioMatrixInsertPatches` + 三个前后缀）。
4. ~~读档后给存量实验室补齐数组~~ —— 已做（`GameData_Import`）。
5. ~~科技追加（幂等）~~ —— 已做，实测追进 32 个科技；**后改为默认关闭**，见 §六。
6. ~~界面第七格~~ —— 已做（`LabSeventhSlotPatches`），见 §八。

**2 到 5 做完就已经可用**——矩阵能被传送带喂进去、能参与研究；
第 6 步只影响「玩家能不能在实验室窗口里看到并手动放入那一格」。

---

## 四、原先「还没定的」三项，现在的结论

| 原议题 | 结论 |
|---|---|
| 第七格的 3D 动画 | **做了。** `techShaderStates[32]` 由 `66666` 改成 `67676`——五个动画位置里两个给生物矩阵。能这么便宜是因为下标 32 恰好等价于「这个科技会消耗生物矩阵」，详见 §七 |
| `PropertySystem.Realize6006AsVariousMatrixToInventory`（宇宙矩阵换各种矩阵） | **不动。** 那是把一个宇宙矩阵拆回六种基础矩阵的便利功能；生物矩阵是宇宙矩阵的**原料**而不是它的拆解产物，把它算进去等于凭空造出一份 |
| 点数（§2.1） | **议题作废。** 科技追加已按 owner 决定砍掉，见 §六 |

---

## 五、宇宙矩阵配方：三处写死的 6

给宇宙矩阵加第七样原料，光改配方表不够——**原版产出模式把六个投料槽写死了**，
而分拣器投不投料只看 `needs[]`：

```
SetFunction         IL 026D–0296:  if (needs.Length != 6) needs = new int[6];  Array.Clear(needs, 0, 6);
UpdateNeedsAssemble IL 0042…00D3:  needs[0] … needs[5]        ← 完全展开，从不写下标 6
UpdateOutputToNext                 叠放实验室之间传 served，同样展开到 0..5
```

没有 needs 槽 ⇒ 永远不被索取 ⇒ 实验室干等一样谁也不会送来的东西，**一条报错都没有**。
和物流站「`storage[0..5]` 展开，超过 6 格货物凭空消失」是同一个形状。

**名单是点过名的**：把全程序碰 `served / needs / incServed` 的方法按「字面下标还是循环」
扫了一遍，产出模式投料链上只有这三处展开，其余全是 `ldlen` 循环。三处都用后缀补齐，
`UpdateNeedsAssemble` 的分批公式逐字抄原版，免得七样原料以不同速度堆积。

**存档安全**：`LabComponent.Import` IL 03A5–03D2 自己会把 `served` / `incServed`
`Array.Resize` 到配方的原料数并保留旧值；`needs` 它不管，由我们的后缀自己兜底。

---

## 六、双重需求：加了又砍

一度是两条并行的需求——科技直接要一份，科技要的宇宙矩阵里又含一份。
**owner 决定砍掉科技那条**：宇宙矩阵配方里已经含了它，科技再要一份就是同一件事收两次费。

砍的方式不是删代码，而是 `lab.json` 的 **`bioMatrixInTechs`（默认 false）**——
这一项已经被改过一次主意，留个不用重新编译就能翻的开关是划算的。

**关键是那三处依赖它的地方全都读末态、不各自写死决定**：

| 依赖处 | 没有科技直接要它时 |
|---|---|
| 科研模式的 `needs[6]` | 置 0，否则研究站会一直往第七格搬永远用不掉的东西 |
| 科研模式画不画第七格 | 不画，否则那格永远不会被消耗，却像是该投料 |
| 3D 动画的前提 | 换成「宇宙矩阵配方里含它」这条路 |

三处读的都是 `UsedByTechs`——从 `LDB.techs` **数出来**的，不是从开关推的。
所以把开关翻回 true，三处自动跟着回来，没有第二个地方需要同步改。

---

## 七、3D 动画的编码

`FactorySystem.GameTickLabResearchMode`（IL 0111–0181）：

```csharp
int state = 0;
foreach (int item in tech.Items) {
    int slot = item - matrixIds[0];                  // = itemId − 6001
    if (slot >= 0 && slot < 5) state |= 1 << slot;   // 前五种：5 位掩码 0..31
    else if (slot == 5) { state = 32; break; }       // 宇宙矩阵：直接跳到「全套」
}
working_length = (float)techShaderStates[clamp(state, 0, 32)] + 0.2f;
```

表值是**五位数字，每位 = 建筑上该位置显示第几种矩阵**。对照原版数据即可确认：
索引 31（前五种全要）= `23514`，五种各一个；索引 32（要宇宙矩阵）= `66666`，五格全是第 6 种。

**于是一个数组元素就够**：`state = 32` 恰好发生在「这个科技要宇宙矩阵」时，
而宇宙矩阵配方里含生物矩阵，所以下标 32 等价于「这个科技会消耗生物矩阵」——
不必转译那个每帧跑的热方法，也不必扩掩码位宽。

**唯一 offline 回答不了的是着色器认不认数字 7**（原版表里从没出现过 7，而着色器是编译好的资产）。
所以：默认图案 `67676` 故意保留三个 6（失败是退化而非空白）、挂在配置开关上（填 0 回原版、不用重编译）、
开没开都打日志。

---

## 八、界面第七格：实测数据与方案

探针（`LabWindowSurvey`）量到的：

```
控件数组长度：itemButtons=6 itemIcons=6 itemIncs=18 itemPercents=6 itemCountTexts=6 itemLocks=6
六格位置（父节点 matrix）：
  [0] (95.0, 21.5)    [1] (-59.0, -90.5)  [2] (59.0, -90.5)
  [3] (-95.0, 21.5)   [4] (0.0, 90.5)     [5] (0.0, -9.5)     尺寸均为 96×96
```

以第 5 格 (0, −9.5) 为圆心重算，前五格是**半径 100 的正五边形**，角度正好 `90° − 72°k`。

### 8.1 为什么只能改成六边形，不能塞进空隙

| 方案 | 相邻中心距 | 控件宽 | 结论 |
|---|---|---|---|
| 塞进正下方的空隙（两边各 36°） | `2×100×sin18° ≈ 61.8` px | 96 px | **会叠** |
| 六边形均分（60°） | `2×100×sin30° = 100` px | 96 px | 留 4 px 缝 ✓ |

### 8.2 布局跟着实际格数走

不是固定六边形——**环上 5 格时摆正五边形，6 格时摆正六边形**：

| 场景 | 环上格数 |
|---|---|
| 合成模式，未选配方（选择界面） | 5 |
| 合成模式，造宇宙矩阵（七样原料） | 6 |
| 合成模式，造别的矩阵 | 5 |
| 科研模式 | 5（`bioMatrixInTechs` 为 true 时 6） |

5 格那一档**精确还原原版**：环上原有五格的顺时针次序没变、起始角仍是 90°、步长回到 72°，
`90 − 72k` 把它们逐个放回本来的位置。

**只按「第七格显示与否」切，不跟解锁状态走**——原版 `_OnUpdate` IL 00D9 会按 `RecipeUnlocked`
隐藏还没解锁的矩阵，前期环上本来就有缺口，那是原版行为。

### 8.3 三个绕开了的坑

- **接线一行不用写。** `_Init` 先调 `_OnInit`（IL 001A）再调 `_OnRegEvent`（IL 0036），
  而后者的接线循环以 `ldlen` 为界——在 `_OnInit` 后缀里扩好数组，第七个按钮由原版自己接线。
- **克隆只克隆根。** 一格八个控件互相嵌套，逐个克隆会得到两份，写数据的和画出来的不是同一个
  （`MultiProductUIPatches` 那次）。另外 `Instantiate(obj, parent)` 默认 `worldPositionStays = true`，
  会改写 `anchoredPosition`，必须用三参重载传 `false`。
- **取不齐就整个回滚。** 数组扩到 7 而 `itemButtons[6]` 为 null，原版接线循环当场 NRE；
  `itemIcons[6]` 为 null 则 `_OnUpdate` 每帧 NRE。两者都比「没有第七格」难查得多。

### 8.4 装饰线

环的父节点下还有个 `lines` 节点，底下两组各 5 根：辐条（2×9.6，半径 50，对准格子）与
短划（28×2，半径 80.7 ≈ `100·cos36°`，在边中点），两组都满足 `旋转 = 角度 − 90`。
按量到的原始值重排，每组补一根备用、5 格时关着。

**相位要在圆上取全组平均，不能取单独一根**：第一根辐条在 (0, 50)，`Atan2` 给的是 89.99999，
于是 `(角度−90)/步长` 是极小的负数，`Mathf.Repeat(…, 1)` 把它卷到 ≈1.0——
相位 0 被当成相位 1，半径按 `cos(60°)/cos(72°)` 算，50 被拉到 80.9。

---

## 九、LDBTool 的 ID 钉子（已踩，记下）

`LDBTool.CustomID.cfg` **按显示名记 ID，并在本 mod 注册之后反向盖回去**。
初版把生物矩阵配成 6644，于是这个号被钉住了：ores.json 改成 6007 之后，
proto 仍被写回 6644，而矩阵表里写的是 6007——
**表指向一个没有 proto 的号，真正的 proto 算出来的槽位是 `6644−6001=643`，
被 `InsertInto` 当场丢弃，一声不吭。**

两处改动：

1. `BioMatrixPatches` 现在**核对末态**——`LDB.items.Select(id).Name` 必须真的是「生物矩阵」，
   否则不启用并说明要删哪个文件的哪两行。宁可不启用，也不扩一张指向空气的表。
2. 处理办法：**先退出游戏**（配置是启动时写的），把 cfg 里那一行的值改成 6007，
   或者删掉该行让 LDBTool 按新值重写。配方号 6644 是对的，不用动——
   **只有物品号有连续性要求**。
