# 催化反应器 V1

> 状态：**第 1~5 步已实现并在游戏里验过**（见文末「实测」）。
> 第八座巨型建筑，`ERecipeType` 取 14——但**14 不是上限**，
> 写稿时那句「最后一个」已查实为错，详见 CLAUDE.md。
> 引擎数字读自 `Assembly-CSharp.dll` v0.10.34.28529 的 IL，或本仓库既有实现；
> 标 **待确认** 的是离线拿不到、必须进游戏验的。

## 零、一句话

一座**记得自己状态**的工厂：催化剂装进床层，跑着跑着积碳失活，机器停下来把
**待生催化剂**吐进物流网，再生炉烧掉积碳把它送回来。整个环是闭的，但有一道缓慢的损耗。

---

## 一、为什么值一个专属的 `ERecipeType`

原版占 1–8 和 15，本 mod 已用 9–13，这座取 14。

> **写稿时这里写的是「14 是最后一个」，那是错的，后来查实了。**
> `ERecipeType` 没有任何上限：枚举没有 `Max` 哨兵、全仓库没有数组按它下标、
> 两个 switch 超范围都落 default、`productionMask` 也不是按类型置位的。16 以上照样能用，
> 完整推导写在 CLAUDE.md。「只剩 14」数的是 `Research(15)` 以下还没用的值，不是天花板——
> **一个看着像上限的数，在找到强制它的代码之前只是个说法。**

真正值得给它一个专属类型的理由是：

> **这是本 mod 第一条有状态的生产。** 在此之前，每台机器都是无记忆的——
> 给够原料就出货，拆了重建一模一样。催化反应器记得自己床里那批催化剂还剩多少活性。

顺带它也是唯一一座**产物会自己变**的建筑：同一条配方，跑着跑着会多吐一样东西出来。

---

## 二、机制：催化剂床

### 2.1 状态

逐建筑三个整数，存在 side table 里（`AlloyRatioStore` 那个「每台建筑一个 `int[]`」的形状）：

| 字段 | 含义 |
|---|---|
| `charge` | 床里装了多少份催化剂 |
| `life` | **还能跑多少个「真的产出了」的 tick** |

活性就是剩余 tick 数本身，**不另设一套 0–10000 的刻度**（写稿时是那样设的）——
面板要显示的「还能跑多久」直接就是它，中间不用再换算一次。

**用一个独立的 store，不要挤进 `AlloyRatioStore`。** 已核过：那个类是
`ConcurrentDictionary<(int PlanetId, int EntityId), int[]>`，**没有任何用途维度**。
一台机器同时是合金机和反应器是不可能的，所以看起来能共用——但 `entityId` 会被回收：
拆掉一台合金机、在同一个 id 上盖起反应器，而 `Remove` 没被调到的话，
那批残留的配比数就会被当成催化剂床读出来。概率低，症状难查，**不值得省这一个类**。

代价是 `IModCanSave` 的流是**位置相关**的，加一个块就要推 `SaveVersion`——
`Export`/`Import`/`IntoOtherSave` 必须同步改，老版本要有迁移分支。这是已知成本，不是风险。

### 2.2 衰减判据 —— 必须复现原版「这一 tick 到底动不动手」

这是钻头那次的教训，原文照抄进来当约束：

> 一个跑在原版**决定要不要动手之前**的钩子，必须复现它的决定，而不是标称速率。

具体到这里，衰减**只在真的产出了的 tick 才发生**。`AssemblerComponent.InternalUpdate`
不产出的情形至少有三种：电力不足、原料不足、产物槽满。任何一种下扣活性，症状都是
「机器停着，催化剂照烧」——**钻头那次玩家报的就是这句话**。

而巨型建筑还多一层：`MegaAssemblerPatches.RunExtraCycles` 每 tick 跑 `cyclesPerTick = 60` 遍。
所以**按炉衰减是错的**：满速下 3600 炉/秒，任何按炉的数都会瞬间见底。
**按 tick 衰减**，和引力透镜的 3600 点是同一个结构。

```
每个「真的产出了的」tick：life--
life <= 0 时：
    把 charge 份 待生催化剂 推进 Supply 槽（推不进去就停在这儿，别把料烧没）
    从 Demand 槽取**整批** chargeSize 份 → charge，life = ticksPerCharge
charge == 0 时：Suppress（机器停转，和没有钻头的采矿机一样）
```

### 2.3 进出料走物流站仓位，**不碰配方数组**

这是本设计最硬的一条约束，来自 `AssemblerComponent.Export`：

> 它按 `recipeExecuteData.products` / `requires` 的**长度**决定写多少条
> `produced` / `served`。**改值安全，改长度坏档。**

而待生催化剂是「跑很久才吐一次」。做成配方的第二产物只有两条路，都不行：
动态改长度 = 坏档；常年留一个 `count = 0` 的产物格 = 界面上挂一个永远是 0 的产物。

**所以走物流站仓位。** 巨型建筑本来就是行星内物流站（30 格），
`MegaStationPatches` 已经在仓位和装配机之间搬东西了：

| 槽 | 方向 | 装什么 |
|---|---|---|
| 催化剂槽 | 本地 Demand | 沸石催化剂，物流网自动送来 |
| 待生槽 | 本地 Supply | 待生催化剂，物流网自动取走 |

**配方数组一个字节都不用动**，存档格式也不用动。

### 2.3a 但不能照抄 `EnsureBitSlot`——我们自己的代码会把那格擦掉

原本打算照采矿机钻头槽那样，从 tick 路径上把仓位填出来。**读了 `MegaStationPatches.SyncStorageLayout`
之后发现这条路是死的**，而且死得很安静：

```csharp
// 按当前配方铺：requires → Demand，products → Supply，cursor 从 0 开始
// 然后把 cursor 之后一直到数组末尾的格子清空
for (; cursor < total; cursor++) {
    if (station.storage[cursor].itemId == 0) continue;
    if (station.storage[cursor].count  > 0) continue;   // <<< 唯一的赦免
    station.storage[cursor].itemId     = 0;
    station.storage[cursor].localLogic = ELogisticStorage.None;
    ...
}
```

那句清理是**对的**（换配方后不清会残留旧物品的需求，而且早先按 12 格建的站点存档里还留着
更长的数组）。但它的赦免条件是 `count > 0` ——

> **催化剂槽恰恰在空的时候才需要存在。** 它空着＝正在向物流网要货，
> 而空着的格子每 tick 都会被我们自己的布局代码擦掉。

症状会是「反应器永远等不到催化剂」，而病因在一个名字里完全没有"催化剂"三个字的方法里。
**这就是钻头那两道闸的形状，而且这次第二道闸又是我们自己的代码**——
CLAUDE.md 里已经记过一次「一条为某座建筑加的规则，悄悄卡住了后来加的另一座」。

**做法：让催化剂槽成为布局的一部分，而不是去和布局抢。**
`SyncStorageLayout` 排完 `requires` 和 `products` 之后，如果这台是催化反应器，
接着用同一个 `cursor` 排催化剂槽（Demand）和待生槽（Supply），再往后才开始清理。
改动集中在一个方法里，且**沿用它现成的 `changed` 返回值**——
只有真的动了才调 `RefreshStationTraffic()`（那要遍历整颗行星的站点）。

**容量要是自己的配置值，不能抄 `storage[0].max`。** 钻头那次栽过：
`StationCapacityPatches` 已经把它抬到 10,005,000，照抄的话第一座反应器会向全网要一千万份
催化剂，把后面每一座都饿死——而症状是「我别的反应器全停了」，指向的地方离病因十万八千里。

### 2.4 V1 不做产量随活性缩放

现实里活性掉了转化率就掉。但那要逐建筑克隆 `recipeExecuteData`（`AlloyRatioPatches` 那一套），
而且得当心「以克隆体为基数再乘一次」导致产量指数跑飞——那条坑 CLAUDE.md 里记着。

**V1 就是二值的：有活性就满速跑，耗尽就停。** 和钻头一模一样，稳、好查、面板纯报数。
产量曲线留给 V2，接口先留出来（`activity` 已经是 0–10000 的连续量）。

---

## 三、化学与内容

### 3.1 催化剂本身

**沸石催化剂**——铝硅酸盐分子筛，水热合成。这是真东西，也是 FCC 和 MTO 工业上真正用的那类。

| | |
|---|---|
| 合成 | 氧化铝矿石 ＋ 硅石 ＋ 水 → 沸石催化剂（化工厂） |
| 失活 | 积碳（coking）：反应过程中重组分在孔道里结焦，堵死活性位 |
| 再生 | 烧掉积碳：**待生催化剂 ＋ 氧气 → 沸石催化剂 ＋ 二氧化碳** |

**再生放在氧化还原化工厂（类型 10），不新开机器**——烧积碳就是氧化，`C + O₂ → CO₂`，
按仓库「机器跟反应类别走」的判据，它本来就该在那儿。真实的 FCC 装置也正是
「反应器—再生器」两个塔，再生器里烧的就是焦炭。

**再生不是 100%。** 真实的沸石在再生温度下会脱铝、水热失活并烧结，工业上每天要补
1–2% 的新鲜剂。所以：

```
待生催化剂 ×10 ＋ 氧气 ×2  →  沸石催化剂 ×9 ＋ 二氧化碳 ×2
```

这一成的损耗就是这条链的**持续需求**——环是闭的，但不是永动的。

**二氧化碳去处是现成的**：`甲醇 · 二氧化碳加氢` 已经在吃它。再生炉的尾气直接喂回甲醇线，
而甲醇又是反应器的原料之一——**这条闭环不是设计出来的，是把真实工艺接上之后自己出现的。**

### 3.2 反应器配方（类型 14）

全部是"分子筛催化 + 必须连续再生"这一族的真实工艺。

| # | 名字 | 原料 → 产物 | 真实依据 |
|---|---|---|---|
| 1 | **乙烯 · 甲醇制烯烃（流化床）** | 甲醇 ×7 → 乙烯 ×2 ＋ 丙烯 ×1 ＋ 水 ×7 | DMTO / UOP-Hydro MTO，SAPO-34。**积碳快到必须连续再生**，这就是为什么它是流化床而不是固定床 |
| 2 | **丙烯 · 催化裂化** | 精炼油 ×6 → 丙烯 ×4 ＋ 乙烯 ×6 | FCC。反应器—再生器的原型 |
| 3 | **氢 · 催化重整** | 精炼油 ×1 ＋ 水 ×4 → 一氧化碳 ×4 ＋ 氢 ×8 | 蒸汽重整，Ni 基催化剂，同样结焦 |

**最终落地的三条碳氢全部严格配平**——写稿时表里那组数**没有配平**，实现时按方程重算了：
第 1 条是 `4 CH₃OH → 2 C₂H₄ + 4 H₂O` 与 `3 CH₃OH → C₃H₆ + 3 H₂O` 合并（7 进 7 出）；
第 2 条因为精炼油、乙烯、丙烯在本 mod 里都是 (CH₂)ₙ，纯裂解守恒到不需要任何配重项
（6×4 = 24 个 CH₂ = 4 丙烯 12 + 6 乙烯 12）；第 3 条是 `C₄H₈ + 4 H₂O → 4 CO + 8 H₂`。

第 1 条和现有的 `6544 乙烯 · 甲醇制烯烃`（化工厂，甲醇 ×2 → 乙烯 ×1 ＋ 水 ×2）**不构成支配**：
产物集合不同——流化床那条同时出丙烯。折成单位甲醇的乙烯产率两条**完全一样**
（7 份甲醇里 4 份走乙烯、3 份走丙烯），
**流化床买到的是丙烯，不是更高的乙烯收率**。这是刻意的：原版那条不能变成死内容。

> 按仓库规矩说清楚：上表「真实依据」那一列是真的（这三条都是教科书级的工艺，
> 而且**结焦—再生**正是它们共同的工程特征，不是为了配合机制编的）。
> 但**投料比是平衡数字**，只有反应类别和产物种类是从化学来的。

### 3.3 丙烯不是死路

新物品只加**丙烯**一个（外加催化剂和待生催化剂）。它的下游是现成的，不用新内容：

丙烯 C₃H₆ 和乙烯 C₂H₄ 一样，裂解给的是**单质碳**而不是 CO，所以它直接落进现有的
`烃热还原` 家族。而那族配方的梯度在本 mod 里本来就是按**碳能给出多少电子**定的
（CO 2、甲醛 4、甲醇 6、乙烯 12）：

```
C₃H₆ → 3C + 3H₂   →  18 个电子，正好是乙烯的 1.5 倍
```

所以 `丙烯 · 烃热还原` 这几条的投料比不用拍脑袋，照着现有乙烯那几条 ×1.5 就是。
**这是本仓库那张 Ellingham 表第一次被用来给新物质自动定位，而不是反过来。**

（丙烯的热值同样按现有锚点算：燃烧焓 2058 kJ/mol，对煤 393.5 kJ/mol ↔ 2.7 MJ，得 14.1 MJ。
乙烯是 9.7 MJ，比值 1.45，和碳数比一致——自洽。）

---

## 四、UI：那块面板的第六个用户，只读

`AlloySliderPatches.cs` 已经有五个用户（合金配比滑条、合金弹药选择器、活性增产剂选择器、
烧结析出、活性复合材）。催化剂床是第六个，而且是**第一个纯只读的**——
按上一轮定的，玩家不选催化剂，所以只要 `RefreshCatalyst`，不要 `HandleCatalystInput`。

落地是**三行加一行结果**：床内装填 / 剩余活性（真的进度条）/ 待生仓，
结果行写「还能跑多少秒、催化剂库存多少」。

**「这一格没排出来」和「排出来了但空着」用不同状态区分**（前者 −1，结果行变红）——
前者是布局坏了，后者是正常在等货，两件事的处理完全不同。

三个已知坑（都是这块面板自己的历史）：

- `LayoutRow` **必须每次刷新都跑**，否则开完另一种机器再开这台会继承上一种的布局
- **不要用 `UnityEngine.UI.Slider` / `Scrollbar`** 画活性条——本仓库为「和 Unity 控件抢一个值
  的所有权」付过三次代价。用两个 `Image`，宽度自己算
- 屏幕→局部坐标走 `UIRoot.ScreenPointIntoRect`

**第二个显示位是白送的**：`UIEntityBriefInfo` 会画 30 格仓位图标，催化剂槽和待生槽自动出现。
但要去核一遍 `StationExpandPatches.StorageCount`——大型采矿机的钻头槽当初就是在这一层
被丢掉的，「数据全对、界面就是不画」。

---

## 五、号段与占位（已核对当前配置）

| | 值 | 依据 |
|---|---|---|
| 建筑物品 / 建造配方 | **6507 / 6507** | 前七座是 6500–6506 配对 |
| 模型号 | **703** | 704/705/708–727 已满，727 是天花板；700–703、706、707 空 |
| `ERecipeType` | **14** | 顺着往下取；**不是上限**，见第一节 |
| 建造栏 | `slot 9`、`gridRow 1`、`gridCol 9` | 1–6、8 是巨型建筑，**7 被 machines.json 的风力发电机集群占着** |
| 新物品 | 6641 沸石催化剂、6642 待生沸石催化剂、6643 丙烯 | 6640 是岩浆 |
| 新配方 | 6648 起 | 6640–6647 已用 |

> 建造栏那一格改之前**必须去 machines.json 核一遍**：两个槽位写在不同文件里，
> 重叠的后果是 `UIBuildMenu.StaticLoad` 后注册的盖掉前一个——**建造栏里直接少一座，不报错**。

---

## 六、分阶段实施

按"慢慢来"拆成五步，每步都能单独验：

| 步 | 内容 | 验收 |
|---|---|---|
| **1** | 本文档 | ✓ |
| **2** | 数据层 | ✓ 注册行齐、翻译不缺 |
| **3** | 图标 ×4 ＋ 程序化网格 | ✓ 1944 三角形，在 788–2036 预算内 |
| **4** | 催化剂床运行时 | ✓ 仓位 4/5 已排出，首次装填 200 份 |
| **5** | 面板（只读）＋ 文档成对改 | ✓ |

第 2 步之后游戏里就能跑起来（机器照常生产，只是还没有催化剂床），
第 4 步才开始真的"要催化剂"。**这个顺序是刻意的**：先让建筑本身被验证过，
再往上叠有状态的逻辑，出问题时能分清是哪一层。

---

## 七、待确认

> 写稿时列的两条最大风险**已经查实并写进上文**，不再是待确认：
> `AlloyRatioStore` 没有用途维度（→ 2.1 改为独立 store），
> `SyncStorageLayout` 会擦掉空的催化剂槽（→ 新增 2.3a）。

1. 巨型建筑的 `Block`（停转）怎么实现最干净——`AlienVeinPatches` 是把 `miningSpeed` 打成 0，
   装配机这边对应的是什么，要读 `MegaAssemblerPatches.MegaTick` 的钩子位置。
   顺带确认「这一 tick 真的产出了吗」从哪里读最可靠（2.2 的衰减判据全靠它）。
2. 模型号 703 进游戏后看 `ResolveModelId` 那行日志，它要是说改用了别的号就照它改 JSON。
3. 原版 MTO / FCC 相关配方的平衡对照——按 `CompositeRegistry.ReportVanillaRoutes` 的先例，
   注册时把精炼油、乙烯现有配方打进启动日志。
4. `SaveVersion` 推一格之后，老存档的迁移分支要按 `AlloyRatioStore.Import(r, version)`
   现成的写法来——那里已经有一次版本迁移的先例可抄。

## 八、参考

- 工艺：DMTO（中科院大连化物所）与 UOP/Hydro MTO，SAPO-34 分子筛；FCC 反应—再生系统；
  蒸汽重整 + 水煤气变换。结焦失活与烧碳再生是这三者共同的工程特征。
- 仓库内先例：`AlienVeinPatches`（消耗判据、仓位布置、Block）、`AlloyRatioStore`（逐建筑状态）、
  `AlloySliderPatches`（面板）、`活性复合材料V1.md` 7.1（下游判据）。

---

## 十、实测（v0.10.34.28529，异常 0 条）

```
催化剂床已就绪：建筑 6507，催化剂 6641／待生 6642，一床 200 份、
                撑 36,000 个产出 tick（满负荷约 600 秒），催化剂槽容量 1,200

巨型建筑物流站接管成功（催化反应器 / 实体 23 / 站点 4）：储物格 30 个
    储物格 0：甲醇             本地 Demand  0/10000000
    储物格 1：乙烯             本地 Supply  0/10000000
    储物格 2：丙烯             本地 Supply  0/10000000
    储物格 3：水               本地 Supply  0/10000000
    储物格 4：沸石催化剂        本地 Demand  0/1200
    储物格 5：待生沸石催化剂    本地 Supply  0/1200

催化剂床：首次装填——200 份，可撑 36,000 个产出 tick

巨型建筑「催化反应器」已换用程序化模型：5832 顶点 / 1944 三角形
```

**两条设计判断被这份日志证实，而不只是被论证：**

1. **催化剂槽排在 requires + products 之后（第 4、5 格）**，没有被 `SyncStorageLayout`
   末尾那个清理循环擦掉——2.3a 那条改法是对的。
2. **容量是 1,200 而不是 10,000,000。** 旁边每一格都是一千万，只有这两格守住了自己的值，
   说明 `StationCapacityPatches` 的排除生效了。**这一条是写完代码才查出来的**：
   那个后置每 tick 把巨型建筑的每一格都刷成 `Config.slotCapacity`，
   刷到本地需求格上就等于让第一座反应器把全网的催化剂吸光——正是 2.3a 末尾
   警告过的那个场景，只不过来源不是 `storage[0].max`，而是另一个每 tick 都在跑的补正。

**还没被实测覆盖的：** 一床跑满 10 分钟之后的失活换料（`首次失活换料` 那行还没出现过），
以及待生仓满时的停转。两者都要等一炉真的跑完。
