# 宣传文案

对外发布用的现成文本。每一块标了用在哪里,可以直接复制。

> **改版本时先跑一遍文末的「数字核对」。** 这个仓库已经有三次把过期的数字发出去过
> ——README 的「五座巨型建筑」、manifest 描述里同一句、两份指南里风力发电机集群的
> 「第 6 格」。**数字要核不要记**,文末给了每个数的来源命令。

---

## 一、一句话

> **把《戴森球计划》的后期整体拉满——而且每一个数值,都是推出来的。**

英文:

> **Scales up the whole late game of Dyson Sphere Program — and every number in it was derived, not picked.**

---

## 二、Thunderstore 短描述

**上限 250 字符,而且必须和 `ProjectEden/manifest.json` 的 `description` 一致。**
`tools/pack_release.py` 打包时会校验长度,但不会校验它和这份文档是否同步——改一处记得改两处。
**Thunderstore 把它当纯文本渲染**,写 `**加粗**` 会原样显示成星号,所以这一段不要带 markdown。

> 把戴森球计划的后期产线整体拉满:六座万倍速巨型建筑、八种新矿脉与完整化工链、九种合金与四维属性、看天吃饭的生物温室、5000 层集装(带 preloader)。每个数值都来自一条真实的化学方程式,而且全在 JSON 里可调。

---

## 三、Thunderstore 页面 / README 长文案(中文)

### 伊甸园计划 Project Eden

**把《戴森球计划》的后期产线整体拉满。**

- **六座万倍速巨型建筑** —— 12 个传送带口直连,不用分拣器;机身自带行星内物流站,30 个储物格、每格 1000 万
- **满级采矿机与物流站**,5000 层传送带集装(原版是 4)
- **八种新矿脉** —— 钴、铝、石膏、锂、锰、铬、钒、钨,按星球主题分布
- **一整条从煤到合金的化工链**,九种合金,每种金属四维属性:硬度 / 韧性 / 耐蚀 / 导电
- **数值全写在 JSON 里,装完就能改**,不用重新编译

#### 但真正不一样的是这个

**这个 mod 里没有一个数是拍出来的。**

- 配方配比来自**配平的化学方程式**:`2 Al₂O₃ + 3 C → 4 Al + 3 CO₂` 就是 矿×2 + 煤×3 → 铝块×4 + 二氧化碳×3
- 热值按**燃烧焓**换算,全表锚在原版煤一个点上(2.7 MJ ↔ 393.5 kJ/mol)
- **哪种还原剂能炼哪种矿,是按 Ellingham 图判的**:一氧化碳还原不了氧化铝,乙烯可以——判据是裂解后给不给得出单质碳
- 用哪台机器看**反应类别**,不看方便:MTO 是脱水不是氧化还原,所以它留在原版化工厂
- 最新加的活性复合材,每个数旁边标着论文出处和 DOI

#### 三个你在别的 mod 里见不到的机制

**配方可以是个秘密。** 合金弹药只有一条配方,但七种合金两两配对有 28 种组合,
每种组合的伤害和产量都不同。游戏里没有任何地方列出"哪两种配哪一档"——这得你自己试。
活性复合材同理:一条配方,六种填料 × 四个等级 = 24 种组合,选料和配比在窗口下面的面板里调。

**有一座建筑看天吃饭。** 生物温室用的是**太阳能板那条日照公式**——满日照满产,
背光面停工,潮汐锁定星球的暗面永久停工。

**合金的配比逐台建筑可调。** 一根滑条,四维属性跟着变,好配比换来更高产量和更短耗时,
代价是每炉多吃稀有金属。

#### 装之前必读

**装了这个 mod 之后存的档,卸载后就打不开了。** 它带一个 preloader,把传送带货物的
两个字段加宽了(这是"高层集装 + 满级增产剂同时成立"的前提),存档格式因此升了版本。
**强烈建议开新档**,或者动手前把存档目录整个复制一份。

#### 其他

- **完整中英双语**,游戏里切语言即可
- **全部美术自绘**,50 张图标用 Python 矢量生成,未使用任何第三方美术资源
- 本项目基于**创世之书(GenesisBook)**开发,代码部分经其作者授权使用
- 代码 GPL-3.0

---

## 四、英文长文案

### Project Eden

**Scales up the whole late game of Dyson Sphere Program — and every number in it was derived, not picked.**

- **Six 10000× mega buildings** — twelve belt ports, no sorters needed, each carrying its own planetary logistics station with 30 slots at 10,000,000 each
- Maxed-out miners and stations, **5000-level belt stacking** (vanilla is 4)
- **Eight new ore veins** — cobalt, aluminium, gypsum, lithium, manganese, chromium, vanadium, tungsten
- A full coal-to-alloy chemistry chain, nine alloys, and a four-axis property row on every metal
- **Every value lives in JSON** and can be edited after install

#### What actually makes it different

**Nothing here is a made-up number.**

Recipe ratios come from **balanced equations** — `2 Al₂O₃ + 3 C → 4 Al + 3 CO₂` is literally
ore ×2 + coal ×3 → aluminium ×4 + CO₂ ×3. Heat values are scaled from **enthalpy of combustion**
against a single vanilla anchor. **Which reductant can smelt which ore is decided by the
Ellingham diagram** — carbon monoxide cannot reduce alumina, ethylene can, and the test is
whether cracking it yields elemental carbon. The newest content cites papers by DOI next to
each value.

#### Three mechanics you won't find elsewhere

**A recipe can be a secret.** Alloy ammunition is one recipe, but seven alloys pair into 28
combinations with different damage and yield — nothing in the game tells you which pair gives
which tier. The Living Composite works the same way: one recipe, six fillers × four grades = 24
combinations, picked on a panel under the assembler window.

**One building depends on the weather.** The Biodome uses **the solar panel's own sunlight
formula**: full output in full sun, nothing on the night side, permanently dark on the far side
of a tidally locked world.

**Alloy ratios are set per building** with a slider, and settle into yield and craft time.

#### Before you install

**A save made with this mod cannot be opened without it.** It ships a preloader that widens two
belt cargo fields — the prerequisite for high stacking and full proliferator coexisting — so the
save format moves up a version. **Start a new save**, or back up your save folder first.

Fully bilingual. All 50 icons drawn from scratch in Python; no third-party art. Built on
**GenesisBook**, whose code is used with the author's permission. GPL-3.0.

---

## 五、中文社区帖(贴吧 / B站 / NGA,口语一些)

> **【MOD】伊甸园计划 —— 后期全面拉满,但每个数都讲得出道理**
>
> 写了个 DSP 的后期扩展 mod,主要是六座万倍速的巨型建筑(自带物流站、12 个带口不用分拣器)、
> 八种新矿脉、一整条从煤到合金的化工链、5000 层集装。
>
> 但我最想说的不是这些。**这个 mod 里没有一个数是我拍脑袋定的。**
>
> 铝为什么要用碳热还原?因为 `2Al₂O₃ + 3C → 4Al + 3CO₂` 是配平的,配比直接照抄。
> 为什么一氧化碳炼不了铝而乙烯可以?查 Ellingham 图——判据是裂解之后能不能给出单质碳。
> 热值怎么定的?按燃烧焓,全表锚在原版煤一个点上。最新加的活性复合材,
> 每个数旁边我都标了论文和 DOI。
>
> 几个自己比较满意的机制:
>
> - **配方可以是秘密**:合金弹药就一条配方,但七种合金两两组合有 28 种,伤害和产量都不一样,
>   游戏里哪都不写,得自己试
> - **有座建筑看天吃饭**:生物温室直接用的太阳能板那条日照公式,背光面停工,
>   潮汐锁定星球的暗面永久停工
> - **合金配比逐台可调**:拖滑条,四维属性跟着动,好配比换产量,代价是多吃稀有金属
>
> ⚠️ **装之前看一眼**:带 preloader,存档格式会升版本,**装了之后的档卸载就打不开了**,
> 建议开新档或者先备份。
>
> 全中英双语,数值全在 JSON 里装完就能改,50 张图标都是自己用 Python 画的。
> 基于创世之书开发,代码部分得到了原作者授权。

---

## 六、数字核对

**发文案之前把这张表跑一遍。** 每个数都有来源,不要凭记忆改。

```bash
python - <<'EOF'
import io, json, os
D = 'ProjectEden/data/'
g = lambda f: json.load(io.open(D + f, encoding='utf-8'))
ores, mega, metals = g('ores.json'), g('megabuildings.json'), g('metals.json')
print('巨型建筑    ', len(mega['buildings']))
print('新矿脉      ', len(ores['ores']))
print('ores 配方   ', len(ores['recipes']) + sum(len(o.get('recipes') or []) for o in ores['ores']))
print('四维属性材料 ', len(metals['metals']))
print('图标        ', len(os.listdir('ProjectEden/assets/icons')))
print('集装层数    ', g('stations.json').get('stationPilerLevel'))
print('配置文件    ', len([f for f in os.listdir(D) if f.endswith('.json')]))
print('i18n 条目   ', len([k for k in g('i18n.json')['strings'] if not k.startswith('//')]))
EOF
```

截至 **1.1.3** 的真值:

| 文案里的说法 | 真值 | 来源 |
|---|---|---|
| 六座万倍速巨型建筑 | **6** | `megabuildings.json` 的 `buildings` |
| 八种新矿脉 | **8** | `ores.json` 的 `ores` |
| 九种合金 | **9** | 设计上的说法;其中碳钢与钛合金映射到原版物品,只在 `metals.json` 里有行 |
| 四维属性 | 覆盖 **22** 种材料 | `metals.json` 的 `metals` |
| 5000 层集装 | **5000** | `stations.json` 的 `stationPilerLevel` |
| 50 张图标 | **50** | `ProjectEden/assets/icons/` 的文件数 |
| 28 种弹药组合 | 7 种合金两两组合 = **28** | `ammo.json` 的候选集 |
| 24 种复合材组合 | 6 填料 × 4 等级 = **24** | `composite.json` 的 `candidates` × `grades` |
| 完整中英双语 | **248** 条对照 | `i18n.json` 的 `strings`(不含 `//` 说明键) |

还要一起改的地方(同一个数字散落在四处):

- `ProjectEden/manifest.json` 的 `description` —— **Thunderstore 页面上就是它**
- `README.md` 的简介段与特性表
- `mod特性.md` / `mod_feature.md` —— 这两份**必须一起改**
- 本文件
