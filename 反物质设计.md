# 反物质产线 · 设计稿

**状态：第一轮实现已落地，等进游戏验证。** 这份文档记的是「决定了什么、为什么、还差什么」。

| 已做 | |
|---|---|
| 6 个物品 | `ores.json`，6677–6682，热值全 0 |
| 6 条配方 | `ores.json`，6687–6692，两条氢副产物带 `energyNote` |
| 4 座巨型建筑 | `megabuildings.json`，物品 6683–6686，`ERecipeType` **19–22** |
| 10 张图标 | `tools/make_icons.py` 里画的矢量，80×80 已进 `assets/icons/` |
| 4 套建模 | `MegaBuildingMeshes.cs`，代码生成，和图标一一对应 |
| 36 条中英对照 | `data/i18n.json`，名字和描述全部成对 |
| **一处基础设施** | `LateItemRef`：`ores.json` 的配方现在能引用 `machines.json` 的物品了，见下 |

| 还没做 | |
|---|---|
| 进游戏验证 | 模型号、格位、`vanilla:` 名字解析、延后引用，四样都只有日志说了算 |
| 逐建筑 `cyclesPerTick` | 第六节 6.1：现在四座跑的还是全局速度，「不是万速」这条**没实现** |
| 分频器 + 供电缩放 | 第六节 6.2 |
| `star.mass` 加成 | 第七节待定 |

**这份文档只有中文，没有英文孪生。** 仓库的双语规则管的是**玩家在游戏里读到的文本**
（物品名、描述、UI 串），和 `部署.md` 一样，内部文档留在中文。真正实现的时候，
物品名和描述照样要成对进 `data/i18n.json`。

**它也不进 `README.md` / `manifest.json` / `CHANGELOG.md`。** 那条规则管的是
「增删了玩家可见内容」的改动，设计稿一样都没增删。等真的实现了再按那份清单走。

---

## 一、链路

一次「炉」的循环产出的东西，正好喂满下游两条支线：

```
吸积熔晶 ×6 ─┐
视界凝核 ×1 ─┼→ 视界蒸发炉 ─┬→ 霍金辐射 ×8 ──→ 磁分离塔 ──┬→ 反质子 ×2
单极磁石 ×2 ─┤              │                  ↑单极磁石×1  └→ 氢 ×6
电浆蓄能柜(满)×1┘            └→ 高能γ光子 ×4 → 对产生室  ──→ 正电子 ×2
                                                ↑碳化钨×1        │
                                                                 ▼
                              反质子 ×2 + 正电子 ×2 ──→ 彭宁阱复合室 ──→ 反物质 ×2
                                        多孔吸气剂 ×1 ↗        └→ 饱和吸气剂 ×1
                                                                      │
        活性复合材 ×3 →[电化学厂]→ 多孔吸气剂 ×2                        │
                        ↑___________[氧化还原厂]← 饱和吸气剂 ×3 ←───────┘
                                     └→ 多孔吸气剂 ×2 + 氢 ×1
```

**新建筑 4 座，新物品 6 个，新配方 6 条。** 吸气剂那两条挂在已有的电化学厂和
氧化还原化工厂上，不新增建筑。

### 建筑

| # | 名字 | `ERecipeType` | 功率 | 输入 → 输出 |
|---|---|---|---|---|
| 1 | **视界蒸发炉**（名字待定） | 19 | 3.0 GW | 吸积熔晶×6 + 视界凝核×1 + 单极磁石×2 + **电浆蓄能柜(满)**×1 → 霍金辐射×8 + 高能γ光子×4 |

> **第四个输入的真名是 `电浆蓄能柜（满）`（物品 6673，179.82 GJ，×333）**，
> 之前一直叫的「电浆基质」这个物品**不存在**。
>
> 旁边还有一档 **`过载蓄能柜（满）`（6675，359.64 GJ，×666）**，而且它的建造配方里
> **就吃视界凝核**——也就是说那一档本来就是黑洞门槛之后才解锁的。用它当炉子的输入
> 在设定上更紧（黑洞的东西喂黑洞的炉子），代价是把两条线绑得更死。**待定。**
| 2 | 磁分离塔 | 20 | 480 MW | 霍金辐射×8 + 单极磁石×1 → 反质子×2 + 氢×6 |
| 3 | 对产生室 | 21 | 360 MW | 高能γ光子×4 + 碳化钨×1 → 正电子×2 |
| 4 | 彭宁阱复合室 | 22 | 300 MW | 反质子×2 + 正电子×2 + 多孔吸气剂×1 → 反物质×2 + 饱和吸气剂×1 |
| — | 电化学厂（已有，type 9） | — | — | 活性复合材×3 → 多孔吸气剂×2 |
| — | 氧化还原化工厂（已有，type 10） | — | — | 饱和吸气剂×3 → 多孔吸气剂×2 + 氢×1 |

`workEnergyPerTick` = 功率 ÷ 60。3.0 GW → `50000000`。

---

## 二、压缩和采集合并成一座,是物理要求不是取舍

原方案是「奇点压缩机 → 微型黑洞 → 霍金辐射采集器」两座建筑加一个中间物品。合并掉了。

**理由：那个黑洞不可能存在足够长的时间被当成物品。**

- 180 GJ ÷ c² = **2.0 mg**
- τ ≈ 8.4×10⁻¹⁷ (M/kg)³ s，代入 2×10⁻⁶ kg → **6.7×10⁻³⁴ s**
- 一个游戏 tick = 1/60 s

黑洞的整个生命周期比引擎能表示的最短时间还短**约 32 个数量级**。所以「微型黑洞是一个
可以放进储物格、放上传送带的物品」在物理上是错的——两座建筑那版才是妥协，合并才是
正确的模型。

合并顺带消掉三件麻烦事，**每一件都是实打实的实现成本**：

- **虚拟物流瞬移问题结构上不存在了。** 巨型建筑本身就是物流站，而
  `MegaVirtualLogisticsPatches` 会在物流站的储物格之间直接搬运、无人机根本不起飞。
  微型黑洞只要是物品，就一定会被瞬移——那比用无人机运更违背「不可运输」的设定。
  现在它不是物品，不用去每一条走 `station.storage` 的逐 tick 通道里排除它
  （催化剂槽位和钻头槽位都为这类事栽过）。
- **`StackSize = 1` 的豁免口子不用开。** 1.10.5 的 `ItemStackSizePatches` 在
  `PostAddDataAction` 末尾把 LDB 里**每一个**物品的 `StackSize` 写成 10000，
  循环里**没有排除表**。要保住「一次只能拿一个」得专门加一个按物品判定的豁免。
- **产物正好卡在原版上限内。** `UIAssemblerWindow` 把产物槽展开到 2 个，所以
  2 产物**不需要 `MultiProductUIPatches`**。4 个输入也在界内——`SyncServingStorage`
  走 `ldlen` 无上限，生物温室已经有 4 输入的先例。

---

## 三、已经量死的数

以下每一条都有日志或 IL 出处，**不是回忆，也不是从注释复述的**。

### 3.1 反物质值多少能量

1.10.6 起的 `EnergyAudit.AuditMegaVanilla` 每局都会打出来（成分是这一轮补的）：

```
「反物质燃料棒」+14376.5 MJ（类型 4）
 ＝ 反物质×12(0) + 氢×12(1.96 MJ) + 湮灭约束球×1(0) + 钛合金×1(0)
 → 反物质燃料棒×2(7200 MJ)
```

| | |
|---|---|
| 反物质自己的 `HeatValue` | **0**（它也不在任何 `FuelType` 组里） |
| 反物质燃料棒 | **7200 MJ**，一次产 2 根 = 14400 MJ |
| 一次棒合成吃 | **反物质 ×12** |
| **每个反物质的能量当量** | **14400 ÷ 12 = 1200 MJ = 1.2 GJ** |

核对：投入侧 12 × 1.96 = 23.52 MJ（氢被本 mod 重锚到 1.96），14400 − 23.5 = 14376.5 ✓

### 3.2 这条链是 75 倍净亏——但能量审计看不见,这两件事要分开说

一个满载电浆蓄能柜 179.82 GJ 出 2 个反物质 → **约 90 GJ 换 1 个**，而它值 1.2 GJ。

```
净亏倍数 = 89910 MJ ÷ 1200 MJ ≈ 75×
```

**反物质是电池，而且是效率极差的电池——这在物理上就该是净亏。**

**但这个 75 倍是个游戏设计事实，不是能量审计查出来的，审计也查不出来。**
实测日志：

```
「电浆蓄能柜（满）」已注册：物品 6673，**不作燃料**
（配置里显式关掉了：容量倍率一大，机甲燃料那一轴就是白送的数值膨胀）
```

它的 `HeatValue` 是 **0**。而 `EnergyAudit.Burnable` 算的就是 `HeatValue × 件数`。
所以 1 号炉那条配方在审计眼里是：

```
投入 0  →  产出 0   差额 = 0
```

**不是「极负」，是「正好为零」。** 整条储能线的 180 GJ 从头到尾不经过任何配方——
电是奇点储能厂从电网抽进柜子的，审计一个焦耳都看不见（`ores.json` 里那条
`//no-heat` 说的就是这件事）。

两个后果，都要记住：

- **好的一面**：这条链构造上不可能触发审计，不需要任何 `energyNote` 豁免。
- **要当心的一面**：**审计在投入侧提供的保护是零。** 它永远不会告诉你这条链是不是净亏，
  因为它把 180 GJ 读成 0。「能量审计通过」这行字对这条链**什么都没验证**。
  平衡只能靠人算，也就是上面那个 75 倍。

  这和已经记过的「`EnergyAudit` 结构上看不见原版配方」是同一族：
  **绿灯的含义比它看起来窄。**

> **产出侧的保护倒是真的在。** 只要中间物一律 `HeatValue = 0`，任何人以后给霍金辐射
> 加热值，1 号炉立刻变成「0 进 N 出」被审计逮住。所以下面那条硬约束仍然是有效防线。

审计会报的**只有两条，都是氢副产物**（阈值 1 MJ）：

| 配方 | 差额 | 理由（要写进 `energyNote`） |
|---|---|---|
| 磁分离塔 | 氢×6 = **+11.8 MJ** | 那 6 个氢是霍金辐射里**物质那一半**；出反质子就必然同时出质子，不是凭空造的 |
| 吸气剂再生 | 氢×1 = **+2.0 MJ** | 同上，来自吸附物的脱附 |

> **硬约束：霍金辐射、高能γ光子、反质子、正电子、多孔吸气剂、饱和吸气剂，
> 一律 `HeatValue = 0`。**
>
> 霍金辐射尤其危险——它读起来就是能量，一旦给了热值，视界蒸发炉那条配方立刻变成
> 「0 进 N 出」的字面永动机，而审计报它会是对的。
>
> 判据是 **`HeatValue`，不是「它算不算燃料」**：`EnergyAudit.Burnable` 第 209 行是
> `proto.HeatValue * counts[i]`，**完全不看 `FuelType`**。

### 3.3 黑洞有几个,有几颗行星

`UniverseGen.CreateGalaxy`：

```
V_9  = CeilToInt(0.01 × 星数 + rand×0.3)     ← 黑洞数
V_13 = 星数 − V_9      // i >= V_13 → BlackHole
```

| 星数 | 黑洞数 |
|---|---|
| 32 | **1** |
| **64（默认）** | **1** |
| 100 | 1–2 |
| 200 | 2–3 |
| 1000 | 10–11 |

`StarGen.CreateStarPlanets` @009B–00A5：

```
ldfld EStarType StarData::type
ldc.i4.4            // BlackHole
bne.un.s -> 00EE
ldc.i4.1
stfld Int32 StarData::planetCount     // = 1，写死，无随机
```

中子星（type 3）@00F7 同样 `planetCount = 1`。白矮星和巨星才有概率分支。

**默认 64 星的一局：整个星系 1 个黑洞，那个黑洞 1 颗行星。**

### 3.4 原料保底必中

`StarVeinPatches` 第 249 行：`byGuarantee = !byRoll && vein.Guarantee && planet.index == 0`。

黑洞只有 1 颗行星 → 那颗必然 `index == 0` → **保底永远命中**。所以：

- 每个黑洞系**必出**吸积熔晶（2 处）和视界凝核（1 处）；
- 每个中子星系**必出**吸积熔晶（2 处）。

「找不找得到」不是问题，保底兜死了，不会重演莫桑石那次。**耗竭也不是问题**——
`forceMiningCostRate: 0` 已经让矿脉不衰减。约束是**产能和运输**。

---

## 四、这条线必然是「在那里采矿,在别处建厂」

黑洞系只有 1 颗行星，既放不下一条完整产线，也**没有任何扩张余地**——那是那个星系
唯一的行星。所以结构上只有一种可能：把矿运出去，在别处建厂。

这把「要探索、要飞过去」从一次性门槛变成了**持续的物流关系**：一颗行星喂整个星系。
而 1.10.7 刚把星际派船从 1 艘/次提到实测 7.42×，这条链的瓶颈会自然落在那条线上——
不是巧合，是同一个约束的两端。

**`star.mass` 的钩子应该挂在这个决策上**：在黑洞旁边建视界蒸发炉拿加成，运出去建拿基准。

> **反过来不行**：如果炉子只能在黑洞系工作，`star.mass` 对每一台实例都是同一个常数，
> 缩放就不是机制，只是换了个数。
>
> 更早的一版想把它挂在「采集器」上，理由是「黑洞质量越小、T_H 越高、蒸发越快」。
> 那个理由**当时就是错的**：黑洞质量由配方定死，和恒星质量无关。合并之后压缩和蒸发
> 在同一座里，星体属性才有了合法的作用点。

---

## 五、物理依据

按仓库的内容规则，每个数都要能追到现实依据，**偏离也要写明**。

| 项 | 依据 |
|---|---|
| 180 GJ → 2 mg | E = mc²，180×10⁹ ÷ (3×10⁸)² = 2.0×10⁻⁶ kg |
| 微型黑洞不是物品 | τ ≈ 8.4×10⁻¹⁷(M/kg)³ s → 6.7×10⁻³⁴ s |
| 霍金辐射出**反质子** | T_H = ħc³/(8πGMk_B) ≈ 1.227×10²³/M(kg) K，2 mg → **6×10²⁸ K**，远在一切粒子质量之上，所以蒸发产物是全谱粒子而不只是光子 |
| 高能γ光子 → 正电子 | 对产生（pair production） |
| 用**碳化钨**做靶 | 对产生截面正比于 **Z²**，高 Z 材料就是干这个用的；钨 Z=74 |
| 吸气剂 | 真空系统里维持超高真空靠的就是吸气剂（getter），吸附饱和后加热再生 |
| 吸气剂 3→2 有损 | 再生每轮都有损耗，是真实的 |
| **蒸发是瞬时的,游戏里给了时间** | 2 mg 黑洞在 10⁻³⁴ s 内释放 180 GJ，功率约 10⁴⁵ W。配方写 35 秒是**游戏抽象**，`//` 里要写明这是偏离 |
| **电子去哪了** | 对产生给出电子-正电子对，电子被靶材吸收。配方只产正电子是简化，`//` 里要写明 |

**吸气剂那条是整套设计里唯一「非物料、而是环境条件」的约束。** 活性复合材不是一次性
投入，而是持续的低速消耗——量不大但断不得，断了反物质产线会因为真空维持不住而停摆。
口感和别的产线不一样，这是故意的。

---

## 六、技术约束（实现前必须知道的）

### 6.1 「巨型建筑但不是万速」目前做不到,而且原因反直觉

`speed` **不能降**——降到 `megaSpeedThreshold` 以下，建筑就永远不再被 `MegaTick`
接管（它靠 `speed >= 阈值` 认身份）。所以节流阀只能是 `cyclesPerTick`。但：

```
speed = 1e8，timeSpend = 配方秒数 × 60 × 10000
35 秒配方 → timeSpend = 21,000,000
每 tick   time += 1e8 ≫ 21,000,000     → 一个 tick 就填满
```

**一旦 speed 是 1e8，配方自己的时间就完全不起作用了。** 每 tick 结算一个周期
= 60 次/秒，无论配方写 8 秒还是 35 秒。

| 建筑 | 配方时间 | 1 倍速 | `cyclesPerTick=1` | 实际倍率 |
|---|---|---|---|---|
| 视界蒸发炉 | 35s | 0.029 次/秒 | 60 次/秒 | **2100×** |
| 磁分离塔 | 10s | 0.1 | 60 | **600×** |
| 对产生室 / 彭宁阱 | 8s | 0.125 | 60 | **480×** |

两件事要做：

1. **`cyclesPerTick` 现在是全局配置**（和 `assemblerSpeed` / `megaSpeedThreshold` /
   `powerScalesCycles` 一起在顶层，一个数管十座）。要给这四座单独的速度，得先把它
   挪进 `MegaBuildingEntry`；读它的地方都在 `MegaAssemblerPatches` 里。
2. **真要慢下来只能跳 tick。** `MegaLightPatches` 已经证明技术可行：在要压制的 tick 上
   把 `time` 预置成 `-speedOverride - 1`，让它加完也够不着 `timeSpend`。
   **两个计时器都得压**——`time`（主产物）和 `extraTime`（增产剂额外产出）是
   `InternalUpdate` 里两个独立的 `if`，只压一个会漏产。

   要 20 倍速的 35 秒配方 ≈ 0.58 次/秒 ≈ 每 103 tick 一个周期。

### 6.2 分频器必须受供电缩放,否则掉电会变成「不降速然后猝死」

`powerScalesCycles` 是按供电比例缩**周期数**，地板 1 cycle/tick。`cyclesPerTick`
一旦设成 1，它就没东西可缩了——建筑满速跑到供电掉破 10%，然后被原版那句
`if (power < 0.1f) return 0` 直接打死。

**而这四座恰恰是最容易掉电的**（3.0 GW 一座）。所以供电比例应当去缩**分频数**，
不是缩周期数——那样掉电时它还有得降，而且降的方向和原版的线性一致
（`InternalUpdate` IL 0576 就是 `time += power × speedOverride`）。

先做固定分频、以后再补供电联动，等于要把这一块重做一遍。

### 6.3 配方时间是装饰,别拿它平衡

承 6.1：在 speed = 1e8 加分频器的组合下，`TimeSpend` **不控制任何吞吐**，
只决定面板显示的秒数和手搓时间。秒数按**可读性**挑。

`matrixTimeSpend` 踩过一模一样的坑，CLAUDE.md 里那句就是为它写的：
「在记录这个旋钮的地方都要说这句话，因为『我改了时间但没变快』读起来就是个 bug」。

### 6.4 耗电是免费的,而且和速度解耦

`workEnergyPerTick` 是**逐建筑**字段、类型 `long`；`SetPCState` 按它算、**不看 `speed`**。
所以「慢而且极耗电」这个组合不需要任何新机制。

现有十座的量级：375000（22.5 MW）到 6000000（360 MW）。3.0 GW = 50000000，是现有
最大值的 8.3 倍，`long` 装得下。

### 6.5 高能γ光子必须是新物品,不能复用临界光子

原版 `质能转换器` 吃**临界光子**直接产反物质。如果 1 号炉产的是临界光子，玩家会直接
走原版那条路，**3、4、5 号建筑当场变成死内容**。

所以它必须是一个独立的 proto。命名上也要能一眼区分。

---

## 七、待定

| 项 | 备选 / 说明 |
|---|---|
| **1 号建筑命名** | 视界蒸发炉 / 奇点蒸发炉 / 视界辐射炉。`奇点` 前缀已被奇点储能厂占用；`视界凝核` 是它的原料，`视界` 呼应得上 |
| **`star.mass` 加成的形式和倍率** | 挂在 1 号炉上，作用于**分频数**（和 6.2 一致）。倍率待定 |
| **吸气剂吃活性复合材的哪一级** | 物理上该吃**低级**那两档（多孔、未刚化）；IV 级刚化之后孔都填死了。这会让四级**非单调**——造复合材是越高级越好，做吸气剂反而低级的好。这是个真实的设计机会，也是个明确的决定，要定下来 |
| **吸气剂再生的损耗率** | 现在是 3→2（1/3）。要不要随级数变 |
| **建造栏格位** | 第 12 类现占 1–6、8–12、14；第 7 格是风力发电机集群（`machines.json`，两文件共用这一类）。**13 空着**，4 座要 13、15、16、17。15 往上超出 10 个可见按钮，靠滚动（`MaxSlot` 36） |

### ID 分配（从 JSON 重推，不是抄 CLAUDE.md 那份账本——那份已经过时）

| | 当前最大 | 本设计取 |
|---|---|---|
| 物品 | 6676 | **6677–6682**（霍金辐射 / 高能γ光子 / 反质子 / 正电子 / 多孔吸气剂 / 饱和吸气剂）|
| 配方 | 6686 | **6687–6692** |
| `ERecipeType` | 已用 1–5、9–14、16–18 | **19、20、21、22** |
| `modelId` | 已钉 691–727（稀疏），ceiling **727** | 可见空位只有 **698、706**，另外两个要从 690 往下拿 |

> **模型号一定要把 `ResolveModelId` 打出来的那行立刻钉进 JSON。**
> 它是**往下扫**的，新建筑插在前面会把后面的全部顶走，而模型号进存档——
> 1.6.5 那次就是七座建筑连环换号。CLAUDE.md 的规矩：
> 「一个 resolver 打出『我挑了另一个值』就是在报告这个号不稳定，第一次看见就钉死」。

---

## 八、已知阻塞（实现前先清）

### 8.1 ~~奇点储能厂的建造栏格位是空的~~ 已查清：假警报，建筑是好的

```
建造栏核对 奇点储能厂：BuildIndex 1214 → 第 12 类第 14 格，格位为空
```

**建筑没问题，坏的是这条检查。** `MegaBuildingRegistry.ReportBuildMenuSlots` 的判据是

```csharp
ItemProto inSlot = cat < 16 && slot >= 0 && slot < 13 ? UIBuildMenu.protos[cat, slot] : null;
string state = inSlot == null ? "格位为空" : ...;
```

`slot < 13` 之外它根本没去读，直接落进 `null` 分支被当成「格位为空」报出来。
**也就是说第 13 格往后的建筑，无论在不在，这条检查印出来的字都一样。**

奇点储能厂实际是好的：`BuildMenuScrollPatches._full` 是**直接从 `BuildIndex` 建的**
（不是从 `protos` 抄的），排到 `MaxSlot = 36`；实测一屏 10 个按钮，第 12 类最大槽位 14
→ 2 页，**第 14 格在第 2 页第 4 位，滚得到**。

这是仓库反复记的那个形状：**读数不随被测的东西变化，就等于没在测**。而且它的来历也是
老形状——`slot < 13` 是照着原版 `StaticLoad` 只填 12 格写的，当时对；
`BuildMenuScrollPatches` 把可用格位扩到 36 之后它就错了，没人回头改。
和钻头槽位撞 `StorageCount`、催化剂槽位撞 `SyncStorageLayout` 同一族：
**本仓库为某个功能加的规则，悄悄限制了后来加的另一个功能。**

**已修**：三种状态现在分得开——第一屏就在 / 要滚到第几页 / **真的没进建造栏**。

> 修的时候差点把假警报换成一个更响的假警报：第一版让检查去问 `_full`，
> 但 `ReportBuildMenuSlots` 跑在**注册期**（紧跟 `UIBuildMenu.StaticLoad`），
> 而 `_full` 是建造栏第一次打开时才建的——那一刻它是 null，"查不到"会被报成
> 「这台建筑进不了建造栏」。判据改成了**静态保证**（`_full` 收 1..36 的任何格位），
> 只有"落在第几页"才去问运行时状态。

### 8.2 星体矿的数量没有任何自检

```
稀有矿脉普查：矿脉表还没建好（已注册 0 种，主题表 就绪），这次跳过，下次再数
```

整局只出现这一次。**它承诺「下次再数」，但没有下次。** 一个跳过了还说自己会重试的
诊断，和一个从没跑过的诊断，在日志里长得一样——本文件记过这个形状。

而且星体矿本来就不走主题表（`RareVeinSurvey` 按 `ThemeProto.RareVeins` 数），
所以那个普查**结构上也看不见**吸积熔晶和视界凝核。也就是说这条链的三个原料里，
**没有一个有数量自检**。

做这条链之前应当补上：按星体类型数一遍候选星系和实际落地的矿点。

### 8.3 重氢的热值没跟着氢一起改（不阻塞，但要记一笔）

```
「重氢」+25.4 MJ（类型 5）＝ 氢×10(1.96 MJ) → 重氢×5(9 MJ)
「氘核燃料棒」+1020 MJ（类型 4）＝ 钛合金×1 + 重氢×20(9 MJ) + 超级磁场环×1 → 氘核燃料棒×2(600 MJ)
```

氢被本 mod 重锚到 1.96，重氢还挂在原版的 9.0，于是分馏塔成了一条
**1.96 → 9 的能量放大器**，而分馏（类型 5）在巨型建筑的可跑列表里。

CLAUDE.md 记过这个形状——「重锚一个物品会给每一条碰它的配方重新定价」——但当时只追到
**氢燃料棒**就停了（用 `vanillaEdits` 的 `setCount` 修掉了）。**重氢是那条链的下一环，
没人跟下去。**

倾向是**不动数，但给它写个理由**：重氢的 9 MJ 根本不是化学热值，是聚变燃料，
而本 mod 的锚是煤（化学）。拿化学锚去套聚变燃料在物理上是错的。
但它现在一条理由都没有，而审计每局都在报它。

---

## 九、被推翻的说法

按仓库的习惯，记下来免得再犯。

| 说过 | 实测 |
|---|---|
| 「微型黑洞 `StackSize=1` + 排除物流搬运，压缩机和采集器必须传送带直连」 | 黑洞寿命 10⁻³⁴ s，**根本不该是物品**；而且 `ItemStackSizePatches` 会把 `StackSize` 压平，豁免要专门开口子 |
| 「`star.mass` 挂在采集器上：黑洞质量越小、T_H 越高、蒸发越快」 | 黑洞质量由配方定死，**和恒星质量无关**。合并之后才有合法的作用点 |
| 「一局里黑洞和中子星本来就只有个位数颗」（`ores.json` 的 `//chance`） | 默认 64 星是 **1 和 1**。「个位数」读起来像「好几颗」 |
| 「吸积熔晶 `chance` 0.5、视界凝核 0.35，所以后者总量约是前者的四分之一」 | **`chance` 在这两种矿上从来没起过作用**——只有 1 颗行星且保底必中，它只决定日志写「掷中」还是「靠首星保底」。四分之一这个**结论**仍然成立，但理由是另一套：(BH×1 处) ÷ ((BH+NS)×2 处)。真正的杠杆是**处数和储量系数** |
| 「反物质是 `FuelType 4`」（`OreConfig.cs` 的注释） | 4 是**反物质燃料棒**和金色燃料棒。反物质本身 `FuelType = 0`、`HeatValue = 0` |
| 「`cyclesPerTick = 1` 就是慢速」 | 是 **480–2100 倍速**。speed = 1e8 之下配方时间完全不起作用 |
| 「输入是**电浆基质**」 | 这个物品**不存在**。真名是 **电浆蓄能柜（满）**（6673，179.82 GJ） |
| 「投入侧是个大可燃值，所以 1 号炉的审计差额极负，构造上安全」 | 差额是**正好 0**——满蓄能柜 `HeatValue = 0`，配置里**显式关掉了燃料属性**。结论（安全）不变，理由整个换了；而且顺带说明**审计在投入侧的保护是零**，它把 180 GJ 读成 0 |
| 「反物质燃料棒 7200 MJ」（从审计差额倒推的） | **实测 7.2 GJ**，推对了，但当时是推的；现在 `FuelSurvey` 每局直接打出来 |
