# 部署

这份文档分两半，各看各的：

- **[第一部分：装给别人用](#第一部分装给别人用)** —— 拿到 zip 的人看这里，可以整段转发
- **[第二部分：打出这个 zip](#第二部分打出这个-zip)** —— 自己发新版时看这里

> 玩家向的完整改动说明在 `mod特性.md` / `mod_feature.md`，那是另一份东西，不在这里重复。

---

# 第一部分：装给别人用

## 一、先装四个前置

在 Thunderstore 里搜，**装进同一个 profile**：

| 前置 | 版本 |
|---|---|
| BepInEx | 5.4.17 |
| LDBTool | 3.0.3 |
| CommonAPI | 1.6.7 |
| DSPModSave | 1.2.2 |

缺任意一个，游戏启动即报错。

## 二、导入本体

**r2modman（推荐）**：左侧 `Settings` → `Import local mod` → 选 zip。

装完必须从 r2modman 点 **`Start modded`** 启动 —— 直接从 Steam 开不会加载任何 mod。

**手动装**：包里 `plugins/` 和 `patchers/` 分别落到 profile 的两个目录下——

```
plugins/*                           →  BepInEx/plugins/ProjectEden/
patchers/ProjectEden.Preloader.dll  →  BepInEx/patchers/ProjectEden/
```

> ⚠️ **`patchers/` 里那个不能漏，也不能放进 `plugins/`。**
> 放错位置**不会报错**：BepInEx 不去 `plugins/` 里找 patcher，插件启动后发现字段没被加宽，
> 就按降级模式继续跑 —— 集装层数退回 255、超过 63 层的增产剂被夹断，
> 日志里写一行就过去了。所以别只拷 `plugins/`。

## 三、⚠️ 开新档，或者先备份

**装了这个 mod 之后存的档，卸载后就打不开了。** 不是「缺图标」那种降级，是真的读不出来。

原因：本 mod 带的 preloader 会在游戏程序集装载前把传送带货物的两个字段加宽
（这是「高层集装 + 满级增产剂同时成立」的前提），货物块的存档格式因此从版本 2 升到 3。

- 装 mod **之前**存的老档仍然能开（按老格式读），**反过来不行**
- 除此之外还会往存档写新物品、新配方、新矿脉类型，以及一个独立的 `.moddsv` 伴生文件
- **没有干净的退回原版的路。要退就回备份。**

存档目录：`%USERPROFILE%\Documents\Dyson Sphere Program\Save`

## 四、怎么确认装对了

看 `<profile>\BepInEx\LogOutput.log`（想在游戏里直接看控制台的话，把
`BepInEx/config/BepInEx.cfg` 里 `[Logging.Console] Enabled` 改成 `true`）。

应该有这两行：

```
Project Eden v1.1.1 已加载
Cargo.inc / Cargo.stack 已加宽为 Int16（结构体 36 字节）：…
```

**第二行是判断 preloader 有没有生效的唯一依据。** 如果看到的是

```
Cargo.inc 仍是 Byte：preloader 没装或整体放弃了改写。
```

说明 `patchers/` 那个 DLL 没到位 —— 游戏照样能玩，但集装层数回到 255。

## 五、想自己改数值

不用重新编译。在

```
<profile>\BepInEx\config\ProjectEden\
```

下放一个同名 json（比如 `stations.json`、`cheats.json`、`ores.json`），mod 会**优先读它**，
并在日志里写一行警告提醒当前用的是外部覆盖。

**六个作弊开关默认全关**，在 `cheats.json` 里：

| 开关 | 作用 |
|---|---|
| `instantBuild` | 建造秒完成（**材料照扣**，只省时间） |
| `noConditionBuild` | 无条件建造（**采矿机除外**） |
| `noCollision` | 建筑可重叠；不碰碰撞体，传送带连接正常 |
| `noCollisionPhysics` | 额外关掉碰撞体池（机甲穿墙）。**会让传送带接不上已有传送带**，非必要别开 |
| `powerNoSpacing` | 发电建筑无间距 |
| `waterPumpAnywhere` | 平地抽水（星球得有水） |

另有总开关 `enabled`（**默认就是开的**，但上面六个全是 `false`，所以实际什么都没启用），
把它改成 `false` 可以一次性禁掉全部。每条开启时都会往日志写一行，
免得以后排查别的问题时把作弊当成 bug。

## 六、出问题怎么反馈

**带上 `LogOutput.log`。**

这个 mod 的每个补丁都会把「匹配到几处」写进日志，匹配为 0 时会直接报 ERROR，
所以那份日志基本能直接定位问题 —— 比截图有用得多。

常见现象对照：

| 现象 | 多半是 |
|---|---|
| 启动即崩，报缺类型 | 前置没装全，或 preloader 版本和游戏版本对不上 |
| 能进游戏但没有新内容 | 没从 r2modman 的 `Start modded` 启动 |
| 集装层数上不去 | `patchers/` 里的 preloader 没装（看上面第四节那行日志） |
| 改了 json 没反应 | 改的是包里那份；要改 `BepInEx/config/ProjectEden/` 下的 |
| 物品栏里找不到新物品 | 物品选取窗口有搜索框，直接打名字；掉落过滤/信号选取窗口确实画不出来，是已知缺口 |

---

# 第二部分：打出这个 zip

## 完整流程

四步，**顺序不能反**：

```bash
# 1. 编译插件（Release）
dotnet build -c Release

# 2. 编译 preloader（Release）—— 它是独立项目，上一步不会带上它
dotnet build ProjectEden.Preloader/ProjectEden.Preloader.csproj -c Release

# 3. 离线校验 preloader —— 必须指定 Release，理由见下
powershell -ExecutionPolicy Bypass -File tools\verify_preloader.ps1 -Config Release

# 4. 打包 —— 不要带参数，默认名就是对的，见下面「包名」
python tools\pack_release.py
```

第 3 步打不出 `all checks passed` 就**不要打包**。

> **fork 下来第一次跑要先告诉它游戏装在哪。** 校验脚本要读 `Assembly-CSharp.dll`，默认路径是原作者那台机器的：
>
> ```powershell
> $env:PROJECTEDEN_MANAGED = "D:\Steam\steamapps\common\Dyson Sphere Program\DSPGAME_Data\Managed"
> # profile 目录默认按 %APPDATA% 推，一般不用管；要改就设 PROJECTEDEN_BEPINEX
> ```
>
> 编译那边是另一套：复制 `DefaultPath.props.example` 成 `DefaultPath.props`（已在 .gitignore 里）。

## 为什么第 3 步不能省

preloader 在游戏程序集交给 CLR 之前重写它。写错的表现是**游戏启动失败、报一个不指向任何本仓库代码的
CLR 类型加载错误** —— 到那一步，本仓库平时那套办法（读 IL、报匹配数、匹配为 0 就大声失败）一个都用不上。

所以改成离线验证：拿真正的 patcher DLL 去改一份 `Assembly-CSharp.dll` 的**拷贝**，写出来，
**再读回来**对着结果断言（而不是相信转换过程自己的报告）。目前十条断言。

游戏更新之后也要重跑 —— 结构性重写会落在「现在那个位置上的字段」，不会报错。

## 为什么必须 `-Config Release`

这个脚本原本硬编码读 `bin\Debug`，而打包取的是 `bin\Release`。

**同一份源码，但「同一份源码」是个论据，不是一次检查。** 发过一次包之后才发现：
那次校验从头到尾没碰过真正发出去的那个二进制。现在脚本加了 `-Config` 参数，
打包前用 `-Config Release`，并且 `pack_release.py` 结束时会再提醒一次。

## 为什么不用 csproj 里那个 `PackageForRelease`

`ProjectEden.csproj` 里有个 `PackageForRelease` target，Release 构建时会产出
`dist/ProjectEden-<版本>.zip`。**那个包不能用来分发**：它是扁平布局（DLL 直接躺在包根），
Thunderstore / r2modman 会把它们全部塞进 `BepInEx/plugins/<包名>/`。

而 preloader **必须落在 `BepInEx/patchers/`**，扁平布局表达不了这件事。
`tools/pack_release.py` 因此改用 `BepInEx/` 目录结构 —— 和 CommonAPI 自己那个 patcher 同一套约定。

那个 target 留着没删，它对「只想拿个插件 DLL」的场合还有用；**但对外分发一律走脚本。**

> 它的输出叫 `ProjectEden-<版本>-plain.zip`。**`-plain` 后缀是故意加的**：
> 原先两者同名，`dotnet build -c Release` 和打包脚本谁后跑谁覆盖，
> 而两个包的布局完全不同。

## 包名

**两个包，成对发布：**

| 包 | 内容 | 给谁 |
|---|---|---|
| `ProjectEden-<版本>.zip` | Thunderstore 布局：`plugins/` + `patchers/` 平级在根，附 LICENSE / NOTICE / 两份特性文档 | 上架用，也是 r2modman 导入用的那个 |
| `ProjectEden-<版本>-plain.zip` | 扁平布局，**不含 preloader**，只有 `ProjectEden.dll` + `Newtonsoft.Json.dll` 加 README / CHANGELOG / manifest / icon | 手动丢进 `BepInEx/plugins/` 的场合 |

**`pack_release.py` 不带参数跑就是对的**——它的默认名正是
`<manifest.name>-<version_number>`。传参数会**覆盖**这个默认，所以别传：
曾经照着本文之前那行日期式的示例传了 `伊甸园计划20260912`，
产出的包和 `dist/` 里历史包对不上队，文件名里也看不出版本。

> **`-plain` 不是 `pack_release.py` 产出的，但也不需要你动手。**
> 它由 `ProjectEden.csproj` 里的 `PackageForRelease` 目标生成（`AfterTargets="Build"`，
> 条件是 `Configuration == Release`）——也就是说**上面第 1 步 `dotnet build -c Release` 跑完它就已经在 `dist/` 里了**。
> 所以四步跑完会得到成对的两个包，不用另外处理。
>
> （本文此处曾写着「仓库里没有生成它的脚本」——那是错的，
> 因为当时只在 `tools/` 里找了一遍，没想到打包逻辑在 csproj 里。
> **找不到生成器时先看构建目标，别只看脚本目录。**）

**版本号必须往上走**，理由见下一节。

## 改版本号要动五处

以 `manifest.json` 为准，其余四处由 `pack_release.py` 在打包前核对，
对不上直接拒绝打包并点名没跟上的文件（已实测会拦）：

| 文件 | 形式 |
|---|---|
| `ProjectEden/manifest.json` | `"version_number"` ← **以它为准** |
| `ProjectEden/ProjectEden.csproj` | `<Version>` |
| `ProjectEden.Preloader/ProjectEden.Preloader.csproj` | `<Version>` |
| `ProjectEden/src/Plugin.cs` | `VERSION = "…"` |
| `ProjectEden/CHANGELOG.md` | `## <版本>` 小节 |

> **还有第六处，脚本拦不住**：`README.md` 第一部分那行「怎么确认装对了」的示例日志
> （`Project Eden v… 已加载`）。它不参与打包校验，写着旧版本号会让人以为装错了。

最容易漏的是 `Plugin.cs` 里那个常量 —— **它是游戏内和日志里显示的版本**，
漏了的话包是新的、日志却报旧版本，排查时会被它带偏。

**版本号一定要往上走。** 同名同版号导入时 r2modman 分不清新旧，
测试者很可能装上去用的还是老的，而且毫无提示。

## 上传 Thunderstore

`pack_release.py` 产出的包就是 Thunderstore 格式，直接传即可。

**包内布局照 DSP 生态的既定约定**：`plugins/` 与 `patchers/` **平级放在包根**，不加 `BepInEx/` 前缀——
CommonAPI 和创世之书的正式包都是这么排的，r2modman 会把它们分别铺到
`BepInEx/plugins/<包名>/` 和 `BepInEx/patchers/<包名>/`。

打包前脚本会先校验 manifest，不合规直接拒绝并点名问题（已实测会拦）：

| 规则 | 说明 |
|---|---|
| `name` | 只能字母/数字/下划线 |
| `version_number` | 必须 `x.y.z`，且**每次上传都要比上一次大** |
| `description` | 非空，**上限 250 字符** |
| `website_url` | 不能为空，否则包页面上没有源码入口 |
| `dependencies` | 非空，且每条是 `命名空间-包名-版本` |
| `icon.png` | **必须正好 256×256** |

依赖串要写**真实发布过的版本**，写错了装的人会卡在依赖解析上：

```
xiaoye97-BepInEx-5.4.17
xiaoye97-LDBTool-3.0.3
CommonAPI-CommonAPI-1.6.7
CommonAPI-DSPModSave-1.2.2
```

## 发布前清单

- [ ] `dotnet build -c Release` 零警告零错误
      （**Release 和 Debug 要分开跑**——Release 多一个打包 target，曾因它引用一个
      已删文件而持续失败，而 Debug 一直是绿的）
- [ ] `verify_preloader.ps1 -Config Release` 十条全过
- [ ] 五处版本号一致（脚本会挡）
- [ ] `CHANGELOG.md` 写了本版条目
- [ ] `manifest.json` 的 `description` 没过期（新增矿脉/建筑的数量最容易写死在里面）
- [ ] `mod特性.md` 与 `mod_feature.md` 标题层级数一致
      （`grep -c '^## '` 与 `grep -c '^### '` 两边相等）
- [ ] 在干净的 profile 里实际导入一次，看第一部分第四节那两行日志是否都在

## 美术资源

`assets/icons/` 下 **77 张图标全部自绘**，由 `tools/make_icons.py` 生成（矢量 → SVG → PNG）：

```bash
python tools\make_icons.py     # 输出到 tools/out/，再拷进 ProjectEden/assets/icons/
```

建筑的几何与贴图由 `ProjectEden/src/Model/` 运行时程序化生成，同样不依赖任何外部素材。

**所以发布包里不含第三方美术资源**，上架前不需要再为素材来源做额外确认。
